---
uid: web-forms/overview/data-access/advanced-data-access-scenarios/creating-stored-procedures-and-user-defined-functions-with-managed-code-cs
title: 관리 코드를 사용 하 여 저장 프로시저 및 사용자 정의 함수C#만들기 () | Microsoft Docs
author: rick-anderson
description: Microsoft SQL Server 2005는 개발자가 관리 코드를 통해 데이터베이스 개체를 만들 수 있도록 .NET 공용 언어 런타임과 통합 됩니다. 이 자습서 ...
ms.author: riande
ms.date: 08/03/2007
ms.assetid: 213eea41-1ab4-4371-8b24-1a1a66c515de
msc.legacyurl: /web-forms/overview/data-access/advanced-data-access-scenarios/creating-stored-procedures-and-user-defined-functions-with-managed-code-cs
msc.type: authoredcontent
ms.openlocfilehash: c6aec9ca70fe3ab568b3d17fea6bfd56671edc03
ms.sourcegitcommit: 22fbd8863672c4ad6693b8388ad5c8e753fb41a2
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 11/28/2019
ms.locfileid: "74605054"
---
# <a name="creating-stored-procedures-and-user-defined-functions-with-managed-code-c"></a><span data-ttu-id="60f92-104">관리 코드를 사용하여 저장 프로시저 및 사용자 정의 함수 만들기(C#)</span><span class="sxs-lookup"><span data-stu-id="60f92-104">Creating Stored Procedures and User-Defined Functions with Managed Code (C#)</span></span>

<span data-ttu-id="60f92-105">[Scott Mitchell](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="60f92-105">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="60f92-106">[코드 다운로드](https://download.microsoft.com/download/3/9/f/39f92b37-e92e-4ab3-909e-b4ef23d01aa3/ASPNET_Data_Tutorial_75_CS.zip) 또는 [PDF 다운로드](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/datatutorial75cs1.pdf)</span><span class="sxs-lookup"><span data-stu-id="60f92-106">[Download Code](https://download.microsoft.com/download/3/9/f/39f92b37-e92e-4ab3-909e-b4ef23d01aa3/ASPNET_Data_Tutorial_75_CS.zip) or [Download PDF](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/datatutorial75cs1.pdf)</span></span>

> <span data-ttu-id="60f92-107">Microsoft SQL Server 2005는 개발자가 관리 코드를 통해 데이터베이스 개체를 만들 수 있도록 .NET 공용 언어 런타임과 통합 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-107">Microsoft SQL Server 2005 integrates with the .NET Common Language Runtime to allow developers to create database objects through managed code.</span></span> <span data-ttu-id="60f92-108">이 자습서에서는 Visual Basic 또는 C# 코드를 사용 하 여 관리 되는 저장 프로시저 및 관리 되는 사용자 정의 함수를 만드는 방법을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-108">This tutorial shows how to create managed stored procedures and managed user-defined functions with your Visual Basic or C# code.</span></span> <span data-ttu-id="60f92-109">또한 이러한 버전의 Visual Studio를 사용 하 여 이러한 관리 되는 데이터베이스 개체를 디버깅 하는 방법을 알아봅니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-109">We also see how these editions of Visual Studio allow you to debug such managed database objects.</span></span>

## <a name="introduction"></a><span data-ttu-id="60f92-110">소개</span><span class="sxs-lookup"><span data-stu-id="60f92-110">Introduction</span></span>

<span data-ttu-id="60f92-111">Microsoft s SQL Server 2005 같은 데이터베이스는 데이터 삽입, 수정 및 검색을 위해 [t-sql (구조적 쿼리 언어 transact-sql)](http://en.wikipedia.org/wiki/Transact-SQL) 을 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-111">Databases like Microsoft s SQL Server 2005 use the [Transact-Structured Query Language (T-SQL)](http://en.wikipedia.org/wiki/Transact-SQL) for inserting, modifying, and retrieving data.</span></span> <span data-ttu-id="60f92-112">대부분의 데이터베이스 시스템은 재사용 가능한 단일 단위로 실행할 수 있는 일련의 SQL 문을 그룹화 하는 구문을 포함 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-112">Most database systems include constructs for grouping a series of SQL statements that can then be executed as a single, reusable unit.</span></span> <span data-ttu-id="60f92-113">저장 프로시저는 한 가지 예입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-113">Stored procedures are one example.</span></span> <span data-ttu-id="60f92-114">또 다른 구문은 udf ( *사용자 정의 함수*)로, 9 단계에서 더 자세히 살펴보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-114">Another is *User-Defined Functions*(UDFs), a construct that we will examine in greater detail in Step 9.</span></span>

<span data-ttu-id="60f92-115">기본적으로 SQL은 데이터 집합을 사용할 수 있도록 설계 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-115">At its core, SQL is designed for working with sets of data.</span></span> <span data-ttu-id="60f92-116">기본적으로 `SELECT`, `UPDATE`및 `DELETE` 문은 해당 테이블의 모든 레코드에 적용 되며, 해당 `WHERE` 절에 의해서만 제한 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-116">The `SELECT`, `UPDATE`, and `DELETE` statements inherently apply to all records in the corresponding table and are only limited by their `WHERE` clauses.</span></span> <span data-ttu-id="60f92-117">그러나 한 번에 하나의 레코드를 사용 하 고 스칼라 데이터를 조작 하기 위해 설계 된 많은 언어 기능이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-117">Yet there are many language features designed for working with one record at a time and for manipulating scalar data.</span></span> <span data-ttu-id="60f92-118">[`CURSOR`](http://www.sqlteam.com/item.asp?ItemID=553) 를 사용 하면 레코드 집합을 한 번에 하나씩 반복할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-118">[`CURSOR` s](http://www.sqlteam.com/item.asp?ItemID=553) allow for a set of records to be looped through one at a time.</span></span> <span data-ttu-id="60f92-119">`LEFT`, `CHARINDEX`및 `PATINDEX`와 같은 문자열 조작 함수는 스칼라 데이터에 사용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-119">String manipulation functions like `LEFT`, `CHARINDEX`, and `PATINDEX` work with scalar data.</span></span> <span data-ttu-id="60f92-120">SQL에는 `IF` 및 `WHILE`같은 제어 흐름 문도 포함 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-120">SQL also includes control flow statements like `IF` and `WHILE`.</span></span>

<span data-ttu-id="60f92-121">Microsoft SQL Server 2005 이전에는 저장 프로시저 및 Udf를 T-sql 문의 컬렉션 으로만 정의할 수 있었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-121">Prior to Microsoft SQL Server 2005, stored procedures and UDFs could only be defined as a collection of T-SQL statements.</span></span> <span data-ttu-id="60f92-122">그러나 SQL Server 2005는 모든 .NET 어셈블리에서 사용 되는 런타임 [CLR (공용 언어 런타임)](https://msdn.microsoft.com/netframework/aa497266.aspx)과의 통합을 제공 하도록 설계 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-122">SQL Server 2005, however, was designed to provide integration with the [Common Language Runtime (CLR)](https://msdn.microsoft.com/netframework/aa497266.aspx), which is the runtime used by all .NET assemblies.</span></span> <span data-ttu-id="60f92-123">따라서 SQL Server 2005 데이터베이스의 저장 프로시저 및 Udf는 관리 코드를 사용 하 여 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-123">Consequently, the stored procedures and UDFs in a SQL Server 2005 database can be created using managed code.</span></span> <span data-ttu-id="60f92-124">즉, C# 클래스에서 저장 프로시저 또는 UDF를 메서드로 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-124">That is, you can create a stored procedure or UDF as a method in a C# class.</span></span> <span data-ttu-id="60f92-125">이렇게 하면 이러한 저장 프로시저 및 Udf를 사용 하 여 .NET Framework 및 사용자 고유의 사용자 지정 클래스의 기능을 활용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-125">This enables these stored procedures and UDFs to utilize functionality in the .NET Framework and from your own custom classes.</span></span>

<span data-ttu-id="60f92-126">이 자습서에서는 관리 되는 저장 프로시저 및 사용자 정의 함수를 만드는 방법과이를 Northwind 데이터베이스에 통합 하는 방법을 살펴봅니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-126">In this tutorial we will examine how to create managed stored procedures and User-Defined Functions and how to integrate them into our Northwind database.</span></span> <span data-ttu-id="60f92-127">S를 시작 하겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-127">Let s get started!</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-128">관리 되는 데이터베이스 개체는 해당 SQL 대응에 대 한 몇 가지 이점을 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-128">Managed database objects offer some advantages over their SQL counterparts.</span></span> <span data-ttu-id="60f92-129">언어 풍부 하 고 친숙 하며 기존 코드 및 논리를 다시 사용 하는 기능이 주요 장점입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-129">Language richness and familiarity and the ability to reuse existing code and logic are the main advantages.</span></span> <span data-ttu-id="60f92-130">하지만 관리 되는 데이터베이스 개체는 많은 절차적 논리를 포함 하지 않는 데이터 집합으로 작업할 때 효율성이 떨어질 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-130">But managed database objects are likely to be less efficient when working with sets of data that do not involve much procedural logic.</span></span> <span data-ttu-id="60f92-131">관리 코드를 사용 하는 경우의 이점에 대 한 자세한 논의는 [관리 코드를 사용 하 여 데이터베이스 개체를 만드는 경우의 이점](https://msdn.microsoft.com/library/k2e1fb36(VS.80).aspx)을 참조 하세요.</span><span class="sxs-lookup"><span data-stu-id="60f92-131">For a more thorough discussion on the advantages of using managed code versus T-SQL, check out the [Advantages of Using Managed Code to Create Database Objects](https://msdn.microsoft.com/library/k2e1fb36(VS.80).aspx).</span></span>

## <a name="step-1-moving-the-northwind-database-out-ofapp_data"></a><span data-ttu-id="60f92-132">1 단계: Northwind 데이터베이스를`App_Data` 외부로 이동</span><span class="sxs-lookup"><span data-stu-id="60f92-132">Step 1: Moving the Northwind Database Out of`App_Data`</span></span>

<span data-ttu-id="60f92-133">지금까지 모든 자습서에서 웹 응용 프로그램의 `App_Data` 폴더에 Microsoft SQL Server 2005 Express Edition 데이터베이스 파일을 사용 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-133">All of our tutorials thus far have used a Microsoft SQL Server 2005 Express Edition database file in the web application s `App_Data` folder.</span></span> <span data-ttu-id="60f92-134">모든 파일은 하나의 디렉터리 내에 있으며 자습서를 테스트 하는 추가 구성 단계가 필요 하지 않으므로 데이터베이스를 `App_Data` 하 여 이러한 자습서를 간단 하 게 배포 하 고 실행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-134">Placing the database in `App_Data` simplified distributing and running these tutorials as all of the files were located within one directory and required no additional configuration steps to test the tutorial.</span></span>

<span data-ttu-id="60f92-135">그러나이 자습서에서는 Northwind 데이터베이스를 `App_Data` 외부로 이동 하 여 SQL Server 2005 Express Edition 데이터베이스 인스턴스에 명시적으로 등록 해 보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-135">For this tutorial, however, let s move the Northwind database out of `App_Data` and explicitly register it with the SQL Server 2005 Express Edition database instance.</span></span> <span data-ttu-id="60f92-136">`App_Data` 폴더의 데이터베이스를 사용 하 여이 자습서에 대 한 단계를 수행할 수 있지만 데이터베이스를 SQL Server 2005 Express Edition 데이터베이스 인스턴스에 명시적으로 등록 하면 많은 단계가 훨씬 더 간단 하 게 수행 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-136">While we can perform the steps for this tutorial with the database in the `App_Data` folder, a number of the steps are made much simpler by explicitly registering the database with the SQL Server 2005 Express Edition database instance.</span></span>

<span data-ttu-id="60f92-137">이 자습서에 대 한 다운로드에는 두 개의 데이터베이스 파일 (`NORTHWND.MDF` `NORTHWND_log.LDF` `DataFiles`라는 폴더에 배치 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-137">The download for this tutorial has the two database files - `NORTHWND.MDF` and `NORTHWND_log.LDF` - placed in a folder named `DataFiles`.</span></span> <span data-ttu-id="60f92-138">자습서의 고유한 구현과 함께 수행 하는 경우 Visual Studio를 닫고 웹 사이트의 `App_Data` 폴더에서 웹 사이트 외부 폴더로 `NORTHWND.MDF` 및 `NORTHWND_log.LDF` 파일을 이동 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-138">If you are following along with your own implementation of the tutorials, close Visual Studio and move the `NORTHWND.MDF` and `NORTHWND_log.LDF` files from the website s `App_Data` folder to a folder outside of the website.</span></span> <span data-ttu-id="60f92-139">데이터베이스 파일을 다른 폴더로 이동한 후에는 Northwind 데이터베이스를 SQL Server 2005 Express Edition 데이터베이스 인스턴스에 등록 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-139">Once the database files have been moved to another folder we need to register the Northwind database with the SQL Server 2005 Express Edition database instance.</span></span> <span data-ttu-id="60f92-140">SQL Server Management Studio에서이 작업을 수행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-140">This can be done from SQL Server Management Studio.</span></span> <span data-ttu-id="60f92-141">2005 SQL Server Express 버전이 아닌 버전이 컴퓨터에 설치 되어 있는 경우 이미 Management Studio 설치 되어 있을 것입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-141">If you have a non-Express Edition of SQL Server 2005 installed on your computer then you likely already have Management Studio installed.</span></span> <span data-ttu-id="60f92-142">컴퓨터에 SQL Server 2005 Express Edition만 있는 경우 [Microsoft SQL Server Management Studio Express](https://www.microsoft.com/downloads/details.aspx?displaylang=en&amp;FamilyID=C243A5AE-4BD1-4E3D-94B8-5A0F62BF7796)를 다운로드 하 여 설치 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-142">If you only have SQL Server 2005 Express Edition on your computer then take a moment to download and install [Microsoft SQL Server Management Studio Express](https://www.microsoft.com/downloads/details.aspx?displaylang=en&amp;FamilyID=C243A5AE-4BD1-4E3D-94B8-5A0F62BF7796).</span></span>

<span data-ttu-id="60f92-143">SQL Server Management Studio를 시작합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-143">Launch SQL Server Management Studio.</span></span> <span data-ttu-id="60f92-144">그림 1에 나와 있는 것 처럼 Management Studio는 연결할 서버를 요청 하는 것으로 시작 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-144">As Figure 1 shows, Management Studio starts by asking what server to connect to.</span></span> <span data-ttu-id="60f92-145">서버 이름에 localhost\SQLExpress를 입력 하 고 인증 드롭다운 목록에서 Windows 인증을 선택한 다음 연결을 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-145">Enter localhost\SQLExpress for the server name, choose Windows Authentication in the Authentication drop-down list, and click Connect.</span></span>

![적절 한 데이터베이스 인스턴스에 연결 합니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image1.png)

<span data-ttu-id="60f92-147">**그림 1**: 해당 데이터베이스 인스턴스에 연결</span><span class="sxs-lookup"><span data-stu-id="60f92-147">**Figure 1**: Connect to the Appropriate Database Instance</span></span>

<span data-ttu-id="60f92-148">연결 되 면 개체 탐색기 창에 해당 데이터베이스, 보안 정보, 관리 옵션 등을 비롯 한 SQL Server 2005 Express Edition 데이터베이스 인스턴스에 대 한 정보가 나열 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-148">Once you ve connected, the Object Explorer window will list information about the SQL Server 2005 Express Edition database instance, including its databases, security information, management options, and so forth.</span></span>

<span data-ttu-id="60f92-149">Northwind 데이터베이스를 `DataFiles` 폴더 (또는 이동 해야 하는 모든 경우)에서 SQL Server 2005 Express Edition 데이터베이스 인스턴스로 연결 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-149">We need to attach the Northwind database in the `DataFiles` folder (or wherever you may have moved it) to the SQL Server 2005 Express Edition database instance.</span></span> <span data-ttu-id="60f92-150">데이터베이스 폴더를 마우스 오른쪽 단추로 클릭 하 고 상황에 맞는 메뉴에서 연결 옵션을 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-150">Right-click on the Databases folder and choose the Attach option from the context menu.</span></span> <span data-ttu-id="60f92-151">그러면 데이터베이스 연결 대화 상자가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-151">This will bring up the Attach Databases dialog box.</span></span> <span data-ttu-id="60f92-152">추가 단추를 클릭 하 고 적절 한 `NORTHWND.MDF` 파일로 드릴 다운 한 다음 확인을 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-152">Click the Add button, drill down to the appropriate `NORTHWND.MDF` file, and click OK.</span></span> <span data-ttu-id="60f92-153">이 시점에서 화면은 그림 2와 유사 하 게 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-153">At this point your screen should look similar to Figure 2.</span></span>

<span data-ttu-id="60f92-154">[적절 한 데이터베이스 인스턴스에 연결 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image3.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image2.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-154">[![Connect to the Appropriate Database Instance](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image3.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image2.png)</span></span>

<span data-ttu-id="60f92-155">**그림 2**: 해당 데이터베이스 인스턴스에 연결 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image4.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-155">**Figure 2**: Connect to the Appropriate Database Instance ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image4.png))</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-156">데이터베이스 연결 대화 상자를 Management Studio를 통해 SQL Server 2005 Express Edition 인스턴스에 연결할 때 내 문서와 같은 사용자 프로필 디렉터리로 드릴 다운할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-156">When connecting to the SQL Server 2005 Express Edition instance through Management Studio the Attach Databases dialog box does not allow you to drill down into user profile directories, such as My Documents.</span></span> <span data-ttu-id="60f92-157">따라서 `NORTHWND.MDF` 및 `NORTHWND_log.LDF` 파일을 사용자가 작성 하지 않은 프로필 디렉터리에 저장 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-157">Therefore, make sure to place the `NORTHWND.MDF` and `NORTHWND_log.LDF` files in a non-user profile directory.</span></span>

<span data-ttu-id="60f92-158">확인 단추를 클릭 하 여 데이터베이스를 연결 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-158">Click the OK button to attach the database.</span></span> <span data-ttu-id="60f92-159">데이터베이스 연결 대화 상자가 닫히고 개체 탐색기에서 이제 연결 된 데이터베이스를 나열 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-159">The Attach Databases dialog box will close and the Object Explorer should now list the just-attached database.</span></span> <span data-ttu-id="60f92-160">Northwind 데이터베이스에 `9FE54661B32FDD967F51D71D0D5145CC_LINE ARTICLES\DATATUTORIALS\VOLUME 3\CSHARP\73\ASPNET_DATA_TUTORIAL_75_CS\APP_DATA\NORTHWND.MDF`와 같은 이름이 있을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-160">Chances are the Northwind database has a name like `9FE54661B32FDD967F51D71D0D5145CC_LINE ARTICLES\DATATUTORIALS\VOLUME 3\CSHARP\73\ASPNET_DATA_TUTORIAL_75_CS\APP_DATA\NORTHWND.MDF`.</span></span> <span data-ttu-id="60f92-161">데이터베이스를 마우스 오른쪽 단추로 클릭 하 고 이름 바꾸기를 선택 하 여 데이터베이스 이름을 Northwind로 바꿉니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-161">Rename the database to Northwind by right-clicking on the database and choosing Rename.</span></span>

![데이터베이스 이름을 Northwind로 바꿉니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image5.png)

<span data-ttu-id="60f92-163">**그림 3**: 데이터베이스를 Northwind로 이름 바꾸기</span><span class="sxs-lookup"><span data-stu-id="60f92-163">**Figure 3**: Rename the Database to Northwind</span></span>

## <a name="step-2-creating-a-new-solution-and-sql-server-project-in-visual-studio"></a><span data-ttu-id="60f92-164">2 단계: Visual Studio에서 새 솔루션 및 SQL Server 프로젝트 만들기</span><span class="sxs-lookup"><span data-stu-id="60f92-164">Step 2: Creating a New Solution and SQL Server Project in Visual Studio</span></span>

<span data-ttu-id="60f92-165">SQL Server 2005에서 관리 되는 저장 프로시저 또는 udf를 만들려면 저장 프로시저 및 UDF 논리를 클래스의 C# 코드로 작성 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-165">To create managed stored procedures or UDFs in SQL Server 2005 we will write the stored procedure and UDF logic as C# code in a class.</span></span> <span data-ttu-id="60f92-166">코드를 작성 한 후에는이 클래스를 어셈블리 (`.dll` 파일)로 컴파일하고 어셈블리를 SQL Server 데이터베이스에 등록 한 다음 어셈블리의 해당 메서드를 가리키는 데이터베이스에 저장 프로시저나 UDF 개체를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-166">Once the code has been written, we will need to compile this class into an assembly (a `.dll` file), register the assembly with the SQL Server database, and then create a stored procedure or UDF object in the database that points to the corresponding method in the assembly.</span></span> <span data-ttu-id="60f92-167">이러한 단계는 모두 수동으로 수행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-167">These steps can all be performed manually.</span></span> <span data-ttu-id="60f92-168">텍스트 편집기에서 코드를 만들고, C# 컴파일러 ([`csc.exe`](https://msdn.microsoft.com/library/ms379563(vs.80).aspx))를 사용 하 여 명령줄에서 컴파일하고, [`CREATE ASSEMBLY`](https://msdn.microsoft.com/library/ms189524.aspx) 명령 또는 Management Studio를 사용 하 여 데이터베이스에 등록 하 고, 비슷한 방법으로 저장 프로시저 또는 UDF 개체를 추가할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-168">We can create the code in any text editor, compile it from the command line using the C# compiler ([`csc.exe`](https://msdn.microsoft.com/library/ms379563(vs.80).aspx)), register it with the database using the [`CREATE ASSEMBLY`](https://msdn.microsoft.com/library/ms189524.aspx) command or from Management Studio, and add the stored procedure or UDF object through similar means.</span></span> <span data-ttu-id="60f92-169">다행히 Visual Studio Professional 및 Team Systems 버전에는 이러한 작업을 자동화 하는 SQL Server 프로젝트 형식이 포함 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-169">Fortunately, the Professional and Team Systems versions of Visual Studio include a SQL Server Project type that automates these tasks.</span></span> <span data-ttu-id="60f92-170">이 자습서에서는 SQL Server 프로젝트 형식을 사용 하 여 관리 되는 저장 프로시저 및 UDF를 만드는 과정을 안내 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-170">In this tutorial we will walk through using the SQL Server Project type to create a managed stored procedure and UDF.</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-171">Visual Studio의 visual Web Developer 또는 Standard edition을 사용 하는 경우에는 수동 방법을 대신 사용 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-171">If you are using Visual Web Developer or the Standard edition of Visual Studio, then you will have to use the manual approach instead.</span></span> <span data-ttu-id="60f92-172">13 단계에서는 이러한 단계를 수동으로 수행 하는 방법에 대 한 자세한 지침을 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-172">Step 13 provides detailed instructions for performing these steps manually.</span></span> <span data-ttu-id="60f92-173">이 단계에서는 사용 중인 Visual Studio 버전에 관계 없이 적용 해야 하는 중요 한 SQL Server 구성 지침을 포함 하므로 13 단계를 읽기 전에 2 ~ 12 단계를 읽어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-173">I encourage you to read Steps 2 through 12 before reading Step 13 since these steps include important SQL Server configuration instructions that must be applied regardless of what version of Visual Studio you are using.</span></span>

<span data-ttu-id="60f92-174">Visual Studio를 열어 시작 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-174">Start by opening Visual Studio.</span></span> <span data-ttu-id="60f92-175">파일 메뉴에서 새 프로젝트를 선택 하 여 새 프로젝트 대화 상자를 표시 합니다 (그림 4 참조).</span><span class="sxs-lookup"><span data-stu-id="60f92-175">From the File menu, choose New Project to display the New Project dialog box (see Figure 4).</span></span> <span data-ttu-id="60f92-176">데이터베이스 프로젝트 형식으로 드릴 다운 한 다음 오른쪽에 나열 된 템플릿에서 새 SQL Server 프로젝트를 만들도록 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-176">Drill down to the Database project type and then, from the Templates listed on the right, choose to create a new SQL Server Project.</span></span> <span data-ttu-id="60f92-177">이 프로젝트의 이름을 `ManagedDatabaseConstructs`로 지정 하 고 `Tutorial75`라는 솔루션에 배치 하도록 선택 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-177">I have chosen to name this project `ManagedDatabaseConstructs` and placed it within a Solution named `Tutorial75`.</span></span>

<span data-ttu-id="60f92-178">[![새 SQL Server 프로젝트 만들기](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image7.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image6.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-178">[![Create a New SQL Server Project](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image7.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image6.png)</span></span>

<span data-ttu-id="60f92-179">**그림 4**: 새 SQL Server 프로젝트 만들기 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image8.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-179">**Figure 4**: Create a New SQL Server Project ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image8.png))</span></span>

<span data-ttu-id="60f92-180">새 프로젝트 대화 상자에서 확인 단추를 클릭 하 여 솔루션 및 SQL Server 프로젝트를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-180">Click the OK button in the New Project dialog box to create the Solution and SQL Server Project.</span></span>

<span data-ttu-id="60f92-181">SQL Server 프로젝트는 특정 데이터베이스에 연결 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-181">A SQL Server Project is tied to a particular database.</span></span> <span data-ttu-id="60f92-182">따라서 새 SQL Server 프로젝트를 만든 후에는이 정보를 지정 하 라는 메시지가 즉시 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-182">Consequently, after creating the new SQL Server Project we are immediately asked to specify this information.</span></span> <span data-ttu-id="60f92-183">그림 5에서는 1 단계에서 다시 SQL Server 2005 Express Edition 데이터베이스 인스턴스에 등록 한 Northwind 데이터베이스를 가리키도록 작성 된 새 데이터베이스 참조 대화 상자를 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-183">Figure 5 shows the New Database Reference dialog box that has been filled out to point to the Northwind database we registered in the SQL Server 2005 Express Edition database instance back in Step 1.</span></span>

![SQL Server 프로젝트를 Northwind 데이터베이스에 연결](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image9.png)

<span data-ttu-id="60f92-185">**그림 5**: SQL Server 프로젝트를 Northwind 데이터베이스에 연결</span><span class="sxs-lookup"><span data-stu-id="60f92-185">**Figure 5**: Associate the SQL Server Project with the Northwind Database</span></span>

<span data-ttu-id="60f92-186">이 프로젝트 내에서 만들 관리 되는 저장 프로시저 및 Udf를 디버깅 하려면 연결에 대해 SQL/CLR 디버깅 지원을 사용 하도록 설정 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-186">In order to debug the managed stored procedures and UDFs we will create within this project, we need to enable SQL/CLR debugging support for the connection.</span></span> <span data-ttu-id="60f92-187">그림 5와 같이 SQL Server 프로젝트를 새 데이터베이스와 연결할 때마다 Visual Studio에서 연결에 대해 SQL/CLR 디버깅을 사용 하도록 설정할지 여부를 묻는 메시지를 표시 합니다 (그림 6 참조).</span><span class="sxs-lookup"><span data-stu-id="60f92-187">Whenever associating a SQL Server Project with a new database (as we did in Figure 5), Visual Studio asks us if we want to enable SQL/CLR debugging on the connection (see Figure 6).</span></span> <span data-ttu-id="60f92-188">예를 클릭합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-188">Click Yes.</span></span>

![SQL/CLR 디버깅 사용](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image10.png)

<span data-ttu-id="60f92-190">**그림 6**: SQL/CLR 디버깅 사용</span><span class="sxs-lookup"><span data-stu-id="60f92-190">**Figure 6**: Enable SQL/CLR Debugging</span></span>

<span data-ttu-id="60f92-191">이제 새 SQL Server 프로젝트가 솔루션에 추가 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-191">At this point the new SQL Server Project has been added to the Solution.</span></span> <span data-ttu-id="60f92-192">이 파일에는 프로젝트에서 만든 관리 되는 데이터베이스 개체를 디버깅 하는 데 사용 되는 `Test.sql`파일을 사용 하 여 `Test Scripts` 이라는 폴더가 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-192">It contains a folder named `Test Scripts` with a file named `Test.sql`, which is used for debugging the managed database objects created in the project.</span></span> <span data-ttu-id="60f92-193">12 단계에서 디버깅을 살펴보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-193">We will look at debugging in Step 12.</span></span>

<span data-ttu-id="60f92-194">이제 새로운 관리 되는 저장 프로시저 및 Udf를이 프로젝트에 추가할 수 있지만, 먼저 솔루션에 기존 웹 응용 프로그램이 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-194">We can now add new managed stored procedures and UDFs to this project, but before we do let s first include our existing web application in the Solution.</span></span> <span data-ttu-id="60f92-195">파일 메뉴에서 추가 옵션을 선택 하 고 기존 웹 사이트를 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-195">From the File menu select the Add option and choose Existing Web Site.</span></span> <span data-ttu-id="60f92-196">해당 웹 사이트 폴더로 이동한 다음 확인을 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-196">Browse to the appropriate website folder and click OK.</span></span> <span data-ttu-id="60f92-197">그림 7에 표시 된 것 처럼이는 웹 사이트와 `ManagedDatabaseConstructs` SQL Server 프로젝트 라는 두 개의 프로젝트를 포함 하도록 솔루션을 업데이트 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-197">As Figure 7 shows, this will update the Solution to include two projects: the website and the `ManagedDatabaseConstructs` SQL Server Project.</span></span>

![현재 솔루션 탐색기에는 두 개의 프로젝트가 포함 되어 있습니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image11.png)

<span data-ttu-id="60f92-199">**그림 7**: 이제 솔루션 탐색기에 두 개의 프로젝트가 포함 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-199">**Figure 7**: The Solution Explorer Now Includes Two Projects</span></span>

<span data-ttu-id="60f92-200">`Web.config`의 `NORTHWNDConnectionString` 값은 현재 `App_Data` 폴더의 `NORTHWND.MDF` 파일을 참조 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-200">The `NORTHWNDConnectionString` value in `Web.config` currently references the `NORTHWND.MDF` file in the `App_Data` folder.</span></span> <span data-ttu-id="60f92-201">`App_Data`에서이 데이터베이스를 제거 하 고 SQL Server 2005 Express Edition 데이터베이스 인스턴스에 명시적으로 등록 했으므로 `NORTHWNDConnectionString` 값을 업데이트 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-201">Since we removed this database from `App_Data` and explicitly registered it in the SQL Server 2005 Express Edition database instance, we need to correspondingly update the `NORTHWNDConnectionString` value.</span></span> <span data-ttu-id="60f92-202">웹 사이트에서 `Web.config` 파일을 열고 `NORTHWNDConnectionString` 값을 변경 하 여 연결 문자열이 `Data Source=localhost\SQLExpress;Initial Catalog=Northwind;Integrated Security=True`를 읽도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-202">Open the `Web.config` file in the website and change the `NORTHWNDConnectionString` value so that the connection string reads: `Data Source=localhost\SQLExpress;Initial Catalog=Northwind;Integrated Security=True`.</span></span> <span data-ttu-id="60f92-203">이 변경 후 `Web.config`의 `<connectionStrings>` 섹션은 다음과 같이 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-203">After this change, your `<connectionStrings>` section in `Web.config` should look similar to the following:</span></span>

[!code-xml[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample1.xml)]

> [!NOTE]
> <span data-ttu-id="60f92-204">[이전 자습서](debugging-stored-procedures-cs.md)에서 설명한 대로 ASP.NET 웹 사이트와 같은 클라이언트 응용 프로그램에서 SQL Server 개체를 디버깅할 때 연결 풀링을 사용 하지 않도록 설정 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-204">As discussed in the [preceding tutorial](debugging-stored-procedures-cs.md), when debugging a SQL Server object from a client application, such as an ASP.NET website, we need to disable connection pooling.</span></span> <span data-ttu-id="60f92-205">위에 표시 된 연결 문자열은 연결 풀링을 사용 하지 않도록 설정 합니다 (`Pooling=false`).</span><span class="sxs-lookup"><span data-stu-id="60f92-205">The connection string shown above disables connection pooling ( `Pooling=false` ).</span></span> <span data-ttu-id="60f92-206">ASP.NET 웹 사이트에서 관리 되는 저장 프로시저 및 Udf를 디버깅 하지 않으려는 경우 연결 풀링을 사용 하도록 설정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-206">If you do not plan on debugging the managed stored procedures and UDFs from the ASP.NET website, enable connection pooling.</span></span>

## <a name="step-3-creating-a-managed-stored-procedure"></a><span data-ttu-id="60f92-207">3 단계: 관리 되는 저장 프로시저 만들기</span><span class="sxs-lookup"><span data-stu-id="60f92-207">Step 3: Creating a Managed Stored Procedure</span></span>

<span data-ttu-id="60f92-208">Northwind 데이터베이스에 관리 되는 저장 프로시저를 추가 하려면 먼저 SQL Server 프로젝트에서 메서드로 저장 프로시저를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-208">To add a managed stored procedure to the Northwind database we first need to create the stored procedure as a method in the SQL Server Project.</span></span> <span data-ttu-id="60f92-209">솔루션 탐색기에서 `ManagedDatabaseConstructs` 프로젝트 이름을 마우스 오른쪽 단추로 클릭 하 고 새 항목을 추가 하도록 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-209">From the Solution Explorer, right-click on the `ManagedDatabaseConstructs` project name and choose to add a new item.</span></span> <span data-ttu-id="60f92-210">그러면 프로젝트에 추가할 수 있는 관리 되는 데이터베이스 개체의 형식을 나열 하는 새 항목 추가 대화 상자가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-210">This will display the Add New Item dialog box, which lists the types of managed database objects that can be added to the project.</span></span> <span data-ttu-id="60f92-211">그림 8에 나와 있는 것 처럼 여기에는 저장 프로시저 및 사용자 정의 함수가 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-211">As Figure 8 shows, this includes stored procedures and User-Defined Functions, among others.</span></span>

<span data-ttu-id="60f92-212">더 이상 사용 되지 않는 모든 제품을 반환 하는 저장 프로시저를 추가 하 여를 시작 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-212">Let s start by adding a stored procedure that simply returns all of the products that have been discontinued.</span></span> <span data-ttu-id="60f92-213">새 저장 프로시저 파일의 이름을 `GetDiscontinuedProducts.cs`합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-213">Name the new stored procedure file `GetDiscontinuedProducts.cs`.</span></span>

<span data-ttu-id="60f92-214">[GetDiscontinuedProducts.cs 라는 새 저장 프로시저를 추가 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image13.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image12.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-214">[![Add a New Stored Procedure Named GetDiscontinuedProducts.cs](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image13.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image12.png)</span></span>

<span data-ttu-id="60f92-215">**그림 8**: 이름이 `GetDiscontinuedProducts.cs` 새 저장 프로시저 추가 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image14.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-215">**Figure 8**: Add a New Stored Procedure Named `GetDiscontinuedProducts.cs` ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image14.png))</span></span>

<span data-ttu-id="60f92-216">그러면 다음 콘텐츠를 사용 C# 하 여 새 클래스 파일이 생성 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-216">This will create a new C# class file with the following content:</span></span>

[!code-csharp[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample2.cs)]

<span data-ttu-id="60f92-217">저장 프로시저는 `StoredProcedures`이라는 `partial` 클래스 파일 내에서 `static` 메서드로 구현 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-217">Note that the stored procedure is implemented as a `static` method within a `partial` class file named `StoredProcedures`.</span></span> <span data-ttu-id="60f92-218">또한 `GetDiscontinuedProducts` 메서드는 메서드를 저장 프로시저로 표시 하는 `SqlProcedure attribute`로 데코 레이트 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-218">Moreover, the `GetDiscontinuedProducts` method is decorated with the `SqlProcedure attribute`, which marks the method as a stored procedure.</span></span>

<span data-ttu-id="60f92-219">다음 코드는 `SqlCommand` 개체를 만들고 해당 `CommandText`를 `Discontinued` 필드가 1 인 제품에 대 한 `Products` 테이블의 모든 열을 반환 하는 `SELECT` 쿼리로 설정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-219">The following code creates a `SqlCommand` object and sets its `CommandText` to a `SELECT` query that returns all of the columns from the `Products` table for products whose `Discontinued` field equals 1.</span></span> <span data-ttu-id="60f92-220">그런 다음 명령을 실행 하 고 결과를 다시 클라이언트 응용 프로그램으로 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-220">It then executes the command and sends the results back to the client application.</span></span> <span data-ttu-id="60f92-221">`GetDiscontinuedProducts` 메서드에이 코드를 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-221">Add this code to the `GetDiscontinuedProducts` method.</span></span>

[!code-csharp[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample3.cs)]

<span data-ttu-id="60f92-222">모든 관리 되는 데이터베이스 개체에는 호출자의 컨텍스트를 나타내는 [`SqlContext` 개체](https://msdn.microsoft.com/library/ms131108.aspx) 에 대 한 액세스 권한이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-222">All managed database objects have access to a [`SqlContext` object](https://msdn.microsoft.com/library/ms131108.aspx) that represents the context of the caller.</span></span> <span data-ttu-id="60f92-223">`SqlContext` [`Pipe` 속성](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlcontext.pipe.aspx)을 통해 [`SqlPipe` 개체](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.aspx) 에 대 한 액세스를 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-223">The `SqlContext` provides access to a [`SqlPipe` object](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.aspx) via its [`Pipe` property](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlcontext.pipe.aspx).</span></span> <span data-ttu-id="60f92-224">이 `SqlPipe` 개체는 SQL Server 데이터베이스와 호출 하는 응용 프로그램 간에 정보를 한쪽 하는 데 사용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-224">This `SqlPipe` object is used to ferry information between the SQL Server database and the calling application.</span></span> <span data-ttu-id="60f92-225">이름에서 알 수 있듯이 [`ExecuteAndSend` 메서드](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.executeandsend.aspx) 는 전달 된 `SqlCommand` 개체를 실행 하 고 결과를 클라이언트 응용 프로그램에 다시 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-225">As its name implies, the [`ExecuteAndSend` method](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlpipe.executeandsend.aspx) executes a passed-in `SqlCommand` object and sends the results back to the client application.</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-226">관리 되는 데이터베이스 개체는 설정 기반 논리가 아닌 절차적 논리를 사용 하는 저장 프로시저 및 Udf에 가장 적합 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-226">Managed database objects are best suited for stored procedures and UDFs that use procedural logic rather than set-based logic.</span></span> <span data-ttu-id="60f92-227">절차적 논리에는 행 단위로 데이터 집합을 사용 하거나 스칼라 데이터로 작업 하는 작업이 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-227">Procedural logic involves working with sets of data on a row-by-row basis or working with scalar data.</span></span> <span data-ttu-id="60f92-228">그러나 방금 만든 `GetDiscontinuedProducts` 메서드는 프로시저 논리를 포함 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-228">The `GetDiscontinuedProducts` method we just created, however, involves no procedural logic.</span></span> <span data-ttu-id="60f92-229">따라서이는 T-sql 저장 프로시저로 구현 하는 것이 가장 좋습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-229">Therefore, it would ideally be implemented as a T-SQL stored procedure.</span></span> <span data-ttu-id="60f92-230">관리 되는 저장 프로시저를 만들고 배포 하는 데 필요한 단계를 보여 주기 위해 관리 되는 저장 프로시저로 구현 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-230">It is implemented as a managed stored procedure to demonstrate the steps necessary for creating and deploying managed stored procedures.</span></span>

## <a name="step-4-deploying-the-managed-stored-procedure"></a><span data-ttu-id="60f92-231">4 단계: 관리 되는 저장 프로시저 배포</span><span class="sxs-lookup"><span data-stu-id="60f92-231">Step 4: Deploying the Managed Stored Procedure</span></span>

<span data-ttu-id="60f92-232">이 코드가 완료 되 면 Northwind 데이터베이스에 배포할 준비가 된 것입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-232">With this code complete, we are ready to deploy it to the Northwind database.</span></span> <span data-ttu-id="60f92-233">SQL Server 프로젝트를 배포 하면 코드를 어셈블리로 컴파일하고, 어셈블리를 데이터베이스에 등록 하 고, 어셈블리의 적절 한 메서드에 연결 하 여 데이터베이스에 해당 개체를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-233">Deploying a SQL Server Project compiles the code into an assembly, registers the assembly with the database, and creates the corresponding objects in the database, linking them to the appropriate methods in the assembly.</span></span> <span data-ttu-id="60f92-234">배포 옵션에 의해 수행 되는 정확한 작업 집합은 13 단계에서 보다 정확 하 게 알 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-234">The exact set of tasks performed by the Deploy option is more precisely spelled out in Step 13.</span></span> <span data-ttu-id="60f92-235">솔루션 탐색기에서 `ManagedDatabaseConstructs` 프로젝트 이름을 마우스 오른쪽 단추로 클릭 하 고 배포 옵션을 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-235">Right-click on the `ManagedDatabaseConstructs` project name in the Solution Explorer and choose the Deploy option.</span></span> <span data-ttu-id="60f92-236">그러나 다음 오류로 인해 배포가 실패 합니다. ' EXTERNAL ' 근처의 구문이 잘못 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-236">However, deployment fails with the following error: Incorrect syntax near 'EXTERNAL'.</span></span> <span data-ttu-id="60f92-237">이 기능을 사용 하려면 현재 데이터베이스의 호환성 수준을 더 높은 값으로 설정 해야 할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-237">You may need to set the compatibility level of the current database to a higher value to enable this feature.</span></span> <span data-ttu-id="60f92-238">`sp_dbcmptlevel`저장 프로시저에 대 한 도움말을 참조 하세요.</span><span class="sxs-lookup"><span data-stu-id="60f92-238">See help for the stored procedure `sp_dbcmptlevel`.</span></span>

<span data-ttu-id="60f92-239">이 오류 메시지는 어셈블리를 Northwind 데이터베이스에 등록 하려고 할 때 발생 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-239">This error message occurs when attempting to register the assembly with the Northwind database.</span></span> <span data-ttu-id="60f92-240">SQL Server 2005 데이터베이스에 어셈블리를 등록 하려면 데이터베이스 호환성 수준을 90으로 설정 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-240">In order to register an assembly with a SQL Server 2005 database, the database s compatibility level must be set to 90.</span></span> <span data-ttu-id="60f92-241">기본적으로 새 SQL Server 2005 데이터베이스의 호환성 수준은 90입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-241">By default, new SQL Server 2005 databases have a compatibility level of 90.</span></span> <span data-ttu-id="60f92-242">그러나 Microsoft SQL Server 2000를 사용 하 여 만든 데이터베이스의 기본 호환성 수준은 80입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-242">However, databases created using Microsoft SQL Server 2000 have a default compatibility level of 80.</span></span> <span data-ttu-id="60f92-243">Northwind 데이터베이스는 처음에 Microsoft SQL Server 2000 데이터베이스 이므로 호환성 수준이 현재 80로 설정 되어 있으므로 관리 되는 데이터베이스 개체를 등록 하려면 90로 늘려야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-243">Since the Northwind database was initially a Microsoft SQL Server 2000 database, its compatibility level is currently set to 80 and therefore needs to be increased to 90 in order to register managed database objects.</span></span>

<span data-ttu-id="60f92-244">데이터베이스 호환성 수준을 업데이트 하려면 Management Studio에서 새 쿼리 창을 열고 다음을 입력 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-244">To update the database s compatibility level, open a New Query window in Management Studio and enter:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample4.sql)]

<span data-ttu-id="60f92-245">위의 쿼리를 실행 하려면 도구 모음에서 실행 아이콘을 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-245">Click the Execute icon in the Toolbar to run the above query.</span></span>

<span data-ttu-id="60f92-246">[Northwind 데이터베이스 호환성 수준을 업데이트 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image16.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image15.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-246">[![Update the Northwind Database s Compatibility Level](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image16.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image15.png)</span></span>

<span data-ttu-id="60f92-247">**그림 9**: Northwind 데이터베이스의 호환성 수준 업데이트 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image17.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-247">**Figure 9**: Update the Northwind Database s Compatibility Level ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image17.png))</span></span>

<span data-ttu-id="60f92-248">호환성 수준을 업데이트 한 후 SQL Server 프로젝트를 다시 배포 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-248">After updating the compatibility level, redeploy the SQL Server Project.</span></span> <span data-ttu-id="60f92-249">이번에는 배포를 오류 없이 완료 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-249">This time the deployment should complete without error.</span></span>

<span data-ttu-id="60f92-250">SQL Server Management Studio로 돌아가서 개체 탐색기에서 Northwind 데이터베이스를 마우스 오른쪽 단추로 클릭 하 고 새로 고침을 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-250">Return to SQL Server Management Studio, right-click on the Northwind database in the Object Explorer, and choose Refresh.</span></span> <span data-ttu-id="60f92-251">그런 다음 프로그래밍 기능 폴더로 드릴 다운 하 고 어셈블리 폴더를 확장 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-251">Next, drill down into the Programmability folder and then expand the Assemblies folder.</span></span> <span data-ttu-id="60f92-252">그림 10에 표시 된 것 처럼 Northwind 데이터베이스에는 `ManagedDatabaseConstructs` 프로젝트에 의해 생성 된 어셈블리가 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-252">As Figure 10 shows, the Northwind database now includes the assembly generated by the `ManagedDatabaseConstructs` project.</span></span>

![이제 ManagedDatabaseConstructs 어셈블리가 Northwind 데이터베이스에 등록 됩니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image18.png)

<span data-ttu-id="60f92-254">**그림 10**: 현재 Northwind 데이터베이스에 등록 된 `ManagedDatabaseConstructs` 어셈블리</span><span class="sxs-lookup"><span data-stu-id="60f92-254">**Figure 10**: The `ManagedDatabaseConstructs` Assembly is Now Registered with the Northwind Database</span></span>

<span data-ttu-id="60f92-255">또한 저장 프로시저 폴더를 확장 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-255">Also expand the Stored Procedures folder.</span></span> <span data-ttu-id="60f92-256">`GetDiscontinuedProducts`이라는 저장 프로시저가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-256">There you will see a stored procedure named `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="60f92-257">이 저장 프로시저는 배포 프로세스에서 만들고 `ManagedDatabaseConstructs` 어셈블리의 `GetDiscontinuedProducts` 메서드를 가리킵니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-257">This stored procedure was created by the deployment process and points to the `GetDiscontinuedProducts` method in the `ManagedDatabaseConstructs` assembly.</span></span> <span data-ttu-id="60f92-258">`GetDiscontinuedProducts` 저장 프로시저를 실행 한 후에는 `GetDiscontinuedProducts` 메서드를 차례로 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-258">When the `GetDiscontinuedProducts` stored procedure is executed, it, in turn, executes the `GetDiscontinuedProducts` method.</span></span> <span data-ttu-id="60f92-259">이는 관리 되는 저장 프로시저 이므로 Management Studio (따라서 저장 프로시저 이름 옆의 잠금 아이콘)을 통해 편집할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-259">Since this is a managed stored procedure it cannot be edited through Management Studio (hence the lock icon next to the stored procedure name).</span></span>

![GetDiscontinuedProducts 저장 프로시저는 저장 프로시저 폴더에 나열 됩니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image19.png)

<span data-ttu-id="60f92-261">**그림 11**: 저장 프로시저 폴더에 `GetDiscontinuedProducts` 저장 프로시저 나열</span><span class="sxs-lookup"><span data-stu-id="60f92-261">**Figure 11**: The `GetDiscontinuedProducts` Stored Procedure is Listed in the Stored Procedures Folder</span></span>

<span data-ttu-id="60f92-262">관리 되는 저장 프로시저를 호출 하기 전에 해결 해야 하는 하나 이상의 장애물 있습니다. 데이터베이스는 관리 코드의 실행을 차단 하도록 구성 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-262">There is still one more hurdle we have to overcome before we can call the managed stored procedure: the database is configured to prevent execution of managed code.</span></span> <span data-ttu-id="60f92-263">새 쿼리 창을 열고 `GetDiscontinuedProducts` 저장 프로시저를 실행 하 여이를 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-263">Verify this by opening a new query window and executing the `GetDiscontinuedProducts` stored procedure.</span></span> <span data-ttu-id="60f92-264">.NET Framework에서 사용자 코드 실행이 사용 하지 않도록 설정 된 다음 오류 메시지가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-264">You will receive the following error message: Execution of user code in the .NET Framework is disabled.</span></span> <span data-ttu-id="60f92-265">Clr 사용 구성 옵션을 사용 하도록 설정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-265">Enable �clr enabled configuration option.</span></span>

<span data-ttu-id="60f92-266">Northwind 데이터베이스 구성 정보를 검사 하려면 쿼리 창에서 `exec sp_configure` 명령을 입력 하 고 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-266">To examine the Northwind database s configuration information, enter and execute the command `exec sp_configure` in the query window.</span></span> <span data-ttu-id="60f92-267">이는 현재 clr 사용 설정이 0으로 설정 되어 있음을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-267">This shows that the clr enabled setting is currently set to 0.</span></span>

<span data-ttu-id="60f92-268">[clr 사용 설정 ![현재 0으로 설정 되어 있습니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image21.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image20.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-268">[![The clr enabled Setting is Currently Set to 0](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image21.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image20.png)</span></span>

<span data-ttu-id="60f92-269">**그림 12**: Clr 사용 설정이 현재 0으로 설정 되어 있습니다 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image22.png)).</span><span class="sxs-lookup"><span data-stu-id="60f92-269">**Figure 12**: The clr enabled Setting is Currently Set to 0 ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image22.png))</span></span>

<span data-ttu-id="60f92-270">그림 12의 각 구성 설정에는 최소 및 최대 값과 구성 및 실행 값의 네 가지 값이 나열 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-270">Note that each configuration setting in Figure 12 has four values listed with it: the minimum and maximum values and the config and run values.</span></span> <span data-ttu-id="60f92-271">Clr 사용 설정의 구성 값을 업데이트 하려면 다음 명령을 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-271">To update the config value for the clr enabled setting, execute the following command:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample5.sql)]

<span data-ttu-id="60f92-272">`exec sp_configure`를 다시 실행 하는 경우 위의 문이 clr enabled 설정의 구성 값을 1로 업데이트 했지만 실행 값이 계속 0으로 설정 된 것을 알 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-272">If you re-run the `exec sp_configure` you will see that the above statement updated the clr enabled setting s config value to 1, but that the run value is still set to 0.</span></span> <span data-ttu-id="60f92-273">이 구성 변경 내용을 적용 하려면 실행 값을 현재 구성 값으로 설정 하는 [`RECONFIGURE` 명령을](https://msdn.microsoft.com/library/ms176069.aspx)실행 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-273">For this configuration change to take affect we need to execute the [`RECONFIGURE` command](https://msdn.microsoft.com/library/ms176069.aspx), which will set the run value to the current config value.</span></span> <span data-ttu-id="60f92-274">쿼리 창에 `RECONFIGURE`를 입력 하 고 도구 모음에서 실행 아이콘을 클릭 하면 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-274">Simply enter `RECONFIGURE` in the query window and click the Execute icon in the Toolbar.</span></span> <span data-ttu-id="60f92-275">이제 `exec sp_configure`를 실행 하는 경우 clr 사용 설정의 구성 및 실행 값에 1 값이 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-275">If you run `exec sp_configure` now you should see a value of 1 for the clr enabled setting s config and run values.</span></span>

<span data-ttu-id="60f92-276">Clr 사용 구성이 완료 되 면 관리 되는 `GetDiscontinuedProducts` 저장 프로시저를 실행할 준비가 된 것입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-276">With the clr enabled configuration complete, we are ready to run the managed `GetDiscontinuedProducts` stored procedure.</span></span> <span data-ttu-id="60f92-277">쿼리 창에서 명령을 입력 하 고 실행 `exec` `GetDiscontinuedProducts`합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-277">In the query window enter and execute the command `exec` `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="60f92-278">저장 프로시저를 호출 하면 `GetDiscontinuedProducts` 메서드의 해당 관리 코드를 실행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-278">Invoking the stored procedure causes the corresponding managed code in the `GetDiscontinuedProducts` method to execute.</span></span> <span data-ttu-id="60f92-279">이 코드는 `SELECT` 쿼리를 실행 하 여 지원 되지 않는 모든 제품을 반환 하 고이 데이터를 호출 하는 응용 프로그램에 반환 합니다 .이는이 인스턴스에 SQL Server Management Studio.</span><span class="sxs-lookup"><span data-stu-id="60f92-279">This code issues a `SELECT` query to return all products that are discontinued and returns this data to the calling application, which is SQL Server Management Studio in this instance.</span></span> <span data-ttu-id="60f92-280">이러한 결과를 수신 하 Management Studio 결과 창에 표시 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-280">Management Studio receives these results and displays them in the Results window.</span></span>

<span data-ttu-id="60f92-281">[GetDiscontinuedProducts 저장 프로시저 ![중단 된 모든 제품을 반환 합니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image24.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image23.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-281">[![The GetDiscontinuedProducts Stored Procedure Returns All Discontinued Products](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image24.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image23.png)</span></span>

<span data-ttu-id="60f92-282">**그림 13**: `GetDiscontinuedProducts` 저장 프로시저에서 지원 되지 않는 모든 제품 반환 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image25.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-282">**Figure 13**: The `GetDiscontinuedProducts` Stored Procedure Returns All Discontinued Products ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image25.png))</span></span>

## <a name="step-5-creating-managed-stored-procedures-that-accept-input-parameters"></a><span data-ttu-id="60f92-283">5 단계: 입력 매개 변수를 허용 하는 관리 되는 저장 프로시저 만들기</span><span class="sxs-lookup"><span data-stu-id="60f92-283">Step 5: Creating Managed Stored Procedures that Accept Input Parameters</span></span>

<span data-ttu-id="60f92-284">이러한 자습서에서 만든 많은 쿼리 및 저장 프로시저에는 *매개 변수가*사용 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-284">Many of the queries and stored procedures we have created throughout these tutorials have used *parameters*.</span></span> <span data-ttu-id="60f92-285">예를 들어 형식화 된 [데이터 집합에 대 한 새 저장 프로시저 만들기의 tableadapter](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-cs.md) 자습서에서 `@CategoryID`라는 입력 매개 변수를 수락한 `GetProductsByCategoryID` 라는 저장 프로시저를 만들었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-285">For example, in the [Creating New Stored Procedures for the Typed DataSet s TableAdapters](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-cs.md) tutorial we created a stored procedure named `GetProductsByCategoryID` that accepted an input parameter named `@CategoryID`.</span></span> <span data-ttu-id="60f92-286">그러면 저장 프로시저가 `CategoryID` 필드가 제공 된 `@CategoryID` 매개 변수의 값과 일치 하는 모든 제품을 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-286">The stored procedure then returned all products whose `CategoryID` field matched the value of the supplied `@CategoryID` parameter.</span></span>

<span data-ttu-id="60f92-287">입력 매개 변수를 허용 하는 관리 되는 저장 프로시저를 만들려면 메서드 정의에서 해당 매개 변수를 지정 하면 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-287">To create a managed stored procedure that accepts input parameters, simply specify those parameters in the method s definition.</span></span> <span data-ttu-id="60f92-288">이를 설명 하기 위해 `GetProductsWithPriceLessThan`이라는 `ManagedDatabaseConstructs` 프로젝트에 다른 관리 되는 저장 프로시저를 추가 해 보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-288">To illustrate this, let s add another managed stored procedure to the `ManagedDatabaseConstructs` project named `GetProductsWithPriceLessThan`.</span></span> <span data-ttu-id="60f92-289">이 관리 되는 저장 프로시저는 가격을 지정 하는 입력 매개 변수를 수락 하 고 `UnitPrice` 필드가 매개 변수 값 보다 작은 모든 제품을 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-289">This managed stored procedure will accept an input parameter specifying a price and will return all products whose `UnitPrice` field is less than the parameter s value.</span></span>

<span data-ttu-id="60f92-290">프로젝트에 새 저장 프로시저를 추가 하려면 `ManagedDatabaseConstructs` 프로젝트 이름을 마우스 오른쪽 단추로 클릭 하 고 새 저장 프로시저를 추가 하도록 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-290">To add a new stored procedure to the project, right-click on the `ManagedDatabaseConstructs` project name and choose to add a new stored procedure.</span></span> <span data-ttu-id="60f92-291">파일 이름을 `GetProductsWithPriceLessThan.cs`로 지정합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-291">Name the file `GetProductsWithPriceLessThan.cs`.</span></span> <span data-ttu-id="60f92-292">3 단계에서 살펴본 것 처럼이는 `partial` 클래스 `StoredProcedures`에 C# 배치 된 `GetProductsWithPriceLessThan` 메서드로 새 클래스 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-292">As we saw in Step 3, this will create a new C# class file with a method named `GetProductsWithPriceLessThan` placed within the `partial` class `StoredProcedures`.</span></span>

<span data-ttu-id="60f92-293">`GetProductsWithPriceLessThan` 메서드 정의를 업데이트 하 여 `price` 라는 [`SqlMoney`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.aspx) 입력 매개 변수를 허용 하 고 코드를 작성 하 여 쿼리 결과를 실행 하 고 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-293">Update the `GetProductsWithPriceLessThan` method s definition so that it accepts a [`SqlMoney`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.aspx) input parameter named `price` and write the code to execute and return the query results:</span></span>

[!code-csharp[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample6.cs)]

<span data-ttu-id="60f92-294">`GetProductsWithPriceLessThan` 메서드 정의와 코드는 3 단계에서 만든 `GetDiscontinuedProducts` 메서드의 정의와 코드와 매우 비슷합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-294">The `GetProductsWithPriceLessThan` method s definition and code closely resembles the definition and code of the `GetDiscontinuedProducts` method created in Step 3.</span></span> <span data-ttu-id="60f92-295">유일한 차이점은 `GetProductsWithPriceLessThan` 메서드는 입력 매개 변수 (`price`)로 수락 하 고, `SqlCommand` s 쿼리에는 매개 변수 (`@MaxPrice`)가 포함 되며, `SqlCommand`의 `Parameters` 컬렉션에 매개 변수가 추가 되 고 `price` 된 변수의 값이 할당 된다는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-295">The only differences are that the `GetProductsWithPriceLessThan` method accepts as input parameter (`price`), the `SqlCommand` s query includes a parameter (`@MaxPrice`), and a parameter is added to the `SqlCommand` s `Parameters` collection is and assigned the value of the `price` variable.</span></span>

<span data-ttu-id="60f92-296">이 코드를 추가한 후 SQL Server 프로젝트를 다시 배포 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-296">After adding this code, redeploy the SQL Server Project.</span></span> <span data-ttu-id="60f92-297">그런 다음 SQL Server Management Studio로 돌아가서 저장 프로시저 폴더를 새로 고칩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-297">Next, return to SQL Server Management Studio and Refresh the Stored Procedures folder.</span></span> <span data-ttu-id="60f92-298">`GetProductsWithPriceLessThan`새 항목이 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-298">You should see a new entry, `GetProductsWithPriceLessThan`.</span></span> <span data-ttu-id="60f92-299">그림 14에 나와 있는 것 처럼 쿼리 창에서 `exec GetProductsWithPriceLessThan 25`명령을 입력 하 고 실행 합니다. 그러면 $25 보다 작은 모든 제품이 나열 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-299">From a query window, enter and execute the command `exec GetProductsWithPriceLessThan 25`, which will list all products less than $25, as Figure 14 shows.</span></span>

<span data-ttu-id="60f92-300">[$25 미만의 ![제품이 표시 됩니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image27.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image26.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-300">[![Products Under $25 are Displayed](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image27.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image26.png)</span></span>

<span data-ttu-id="60f92-301">**그림 14**: $25 아래의 제품이 표시 됩니다 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image28.png)).</span><span class="sxs-lookup"><span data-stu-id="60f92-301">**Figure 14**: Products Under $25 are Displayed ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image28.png))</span></span>

## <a name="step-6-calling-the-managed-stored-procedure-from-the-data-access-layer"></a><span data-ttu-id="60f92-302">6 단계: 데이터 액세스 계층에서 관리 되는 저장 프로시저 호출</span><span class="sxs-lookup"><span data-stu-id="60f92-302">Step 6: Calling the Managed Stored Procedure from the Data Access Layer</span></span>

<span data-ttu-id="60f92-303">이 시점에서 `GetDiscontinuedProducts` 및 `GetProductsWithPriceLessThan` 관리 되는 저장 프로시저를 `ManagedDatabaseConstructs` 프로젝트에 추가 하 고 Northwind SQL Server 데이터베이스에 등록 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-303">At this point we have added the `GetDiscontinuedProducts` and `GetProductsWithPriceLessThan` managed stored procedures to the `ManagedDatabaseConstructs` project and have registered them with the Northwind SQL Server database.</span></span> <span data-ttu-id="60f92-304">또한 SQL Server Management Studio에서 이러한 관리 되는 저장 프로시저를 호출 했습니다 (그림 s 13 및 14 참조).</span><span class="sxs-lookup"><span data-stu-id="60f92-304">We also invoked these managed stored procedures from SQL Server Management Studio (see Figure s 13 and 14).</span></span> <span data-ttu-id="60f92-305">그러나 ASP.NET 응용 프로그램은 이러한 관리 되는 저장 프로시저를 사용 하기 위해 아키텍처의 데이터 액세스 및 비즈니스 논리 계층에 추가 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-305">In order for our ASP.NET application to use these managed stored procedures, however, we need to add them to the Data Access and Business Logic Layers in the architecture.</span></span> <span data-ttu-id="60f92-306">이 단계에서는 형식화 된 [데이터 집합의 tableadapter 자습서에 대 한 새 저장 프로시저 만들기](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-cs.md) 에서 처음에 만든 `NorthwindWithSprocs` 형식화 된 데이터 집합의 `ProductsTableAdapter`에 두 개의 새 메서드를 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-306">In this step we will add two new methods to the `ProductsTableAdapter` in the `NorthwindWithSprocs` Typed DataSet, which was initially created in the [Creating New Stored Procedures for the Typed DataSet s TableAdapters](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-cs.md) tutorial.</span></span> <span data-ttu-id="60f92-307">7 단계에서는 해당 하는 메서드를 BLL에 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-307">In Step 7 we will add corresponding methods to the BLL.</span></span>

<span data-ttu-id="60f92-308">Visual Studio에서 `NorthwindWithSprocs` 형식화 된 데이터 집합을 열고 `GetDiscontinuedProducts`라는 `ProductsTableAdapter`에 새 메서드를 추가 하 여 시작 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-308">Open the `NorthwindWithSprocs` Typed DataSet in Visual Studio and start by adding a new method to the `ProductsTableAdapter` named `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="60f92-309">TableAdapter에 새 메서드를 추가 하려면 디자이너에서 TableAdapter s 이름을 마우스 오른쪽 단추로 클릭 하 고 상황에 맞는 메뉴에서 쿼리 추가 옵션을 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-309">To add a new method to a TableAdapter, right-click on the TableAdapter s name in the Designer and choose the Add Query option from the context menu.</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-310">Northwind 데이터베이스를 `App_Data` 폴더에서 SQL Server 2005 Express Edition 데이터베이스 인스턴스로 이동 했으므로 web.config의 해당 연결 문자열이이 변경 내용을 반영 하도록 업데이트 되어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-310">Since we moved the Northwind database from the `App_Data` folder to the SQL Server 2005 Express Edition database instance, it is imperative that the corresponding connection string in Web.config be updated to reflect this change.</span></span> <span data-ttu-id="60f92-311">2 단계에서는 `Web.config`의 `NORTHWNDConnectionString` 값을 업데이트 하는 방법을 설명 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-311">In Step 2 we discussed updating the `NORTHWNDConnectionString` value in `Web.config`.</span></span> <span data-ttu-id="60f92-312">이 업데이트를 수행 하지 않은 경우 쿼리를 추가 하지 못했습니다 라는 오류 메시지가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-312">If you forgot to make this update, then you will see the error message Failed to add query.</span></span> <span data-ttu-id="60f92-313">TableAdapter에 새 메서드를 추가 하려고 할 때 대화 상자에서 개체 `Web.config`에 대 한 연결 `NORTHWNDConnectionString`를 찾을 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-313">Unable to find connection `NORTHWNDConnectionString` for object `Web.config` in a dialog box when attempting to add a new method to the TableAdapter.</span></span> <span data-ttu-id="60f92-314">이 오류를 해결 하려면 확인을 클릭 한 다음 `Web.config`로 이동 하 고 2 단계에서 설명한 대로 `NORTHWNDConnectionString` 값을 업데이트 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-314">To resolve this error, click OK and then go to `Web.config` and update the `NORTHWNDConnectionString` value as discussed in Step 2.</span></span> <span data-ttu-id="60f92-315">그런 다음 메서드를 TableAdapter에 다시 추가 해 봅니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-315">Then try re-adding the method to the TableAdapter.</span></span> <span data-ttu-id="60f92-316">이번에는 오류 없이 작동 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-316">This time it should work without error.</span></span>

<span data-ttu-id="60f92-317">새 메서드를 추가 하면 이전 자습서에서 여러 번 사용한 TableAdapter 쿼리 구성 마법사가 시작 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-317">Adding a new method launches the TableAdapter Query Configuration wizard, which we have used many times in past tutorials.</span></span> <span data-ttu-id="60f92-318">첫 번째 단계에서는 TableAdapter가 데이터베이스에 액세스 하는 방법을 지정 하 라는 메시지를 표시 합니다. 즉, 임시 SQL 문이나 기존 또는 기존 저장 프로시저를 통해 데이터베이스에 액세스 하는 방법을 지정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-318">The first step asks us to specify how the TableAdapter should access the database: through an ad-hoc SQL statement or via a new or existing stored procedure.</span></span> <span data-ttu-id="60f92-319">데이터베이스에 `GetDiscontinuedProducts` 관리 저장 프로시저를 이미 만들고 등록 했으므로 기존 저장 프로시저 사용 옵션을 선택 하 고 다음에 적중 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-319">Since we have already created and registered the `GetDiscontinuedProducts` managed stored procedure with the database, choose the Use existing stored procedure option and hit Next.</span></span>

<span data-ttu-id="60f92-320">[![기존 저장 프로시저 사용 옵션을 선택 합니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image30.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image29.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-320">[![Choose the Use existing stored procedure Option](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image30.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image29.png)</span></span>

<span data-ttu-id="60f92-321">**그림 15**: 기존 저장 프로시저 사용 옵션 선택 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image31.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-321">**Figure 15**: Choose the Use existing stored procedure Option ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image31.png))</span></span>

<span data-ttu-id="60f92-322">다음 화면에는 메서드가 호출 하는 저장 프로시저에 대 한 메시지가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-322">The next screen prompts us for the stored procedure the method will invoke.</span></span> <span data-ttu-id="60f92-323">드롭다운 목록에서 `GetDiscontinuedProducts` 관리 되는 저장 프로시저를 선택 하 고 다음을 누릅니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-323">Choose the `GetDiscontinuedProducts` managed stored procedure from the drop-down list and hit Next.</span></span>

<span data-ttu-id="60f92-324">[GetDiscontinuedProducts 관리 되는 저장 프로시저 ![선택 합니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image33.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image32.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-324">[![Select the GetDiscontinuedProducts Managed Stored Procedure](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image33.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image32.png)</span></span>

<span data-ttu-id="60f92-325">**그림 16**: 관리 되는 저장 프로시저 `GetDiscontinuedProducts` 선택 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image34.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-325">**Figure 16**: Select the `GetDiscontinuedProducts` Managed Stored Procedure ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image34.png))</span></span>

<span data-ttu-id="60f92-326">그런 다음 저장 프로시저에서 행, 단일 값 또는 nothing을 반환 하는지 여부를 지정 하 라는 메시지가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-326">We are then asked to specify whether the stored procedure returns rows, a single value, or nothing.</span></span> <span data-ttu-id="60f92-327">`GetDiscontinuedProducts`에서 지원 되지 않는 제품 행 집합을 반환 하기 때문에 첫 번째 옵션 (테이블 형식 데이터)을 선택 하 고 다음을 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-327">Since `GetDiscontinuedProducts` returns the set of discontinued product rows, choose the first option ( Tabular data ) and click Next.</span></span>

<span data-ttu-id="60f92-328">[테이블 형식 데이터 옵션을 선택 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image36.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image35.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-328">[![Select the Tabular Data Option](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image36.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image35.png)</span></span>

<span data-ttu-id="60f92-329">**그림 17**: 테이블 형식 데이터 옵션 선택 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image37.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-329">**Figure 17**: Select the Tabular Data Option ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image37.png))</span></span>

<span data-ttu-id="60f92-330">최종 마법사 화면에서는 사용 된 데이터 액세스 패턴 및 결과 메서드의 이름을 지정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-330">The final wizard screen allows us to specify the data access patterns used and the names of the resulting methods.</span></span> <span data-ttu-id="60f92-331">두 확인란을 모두 선택 된 채로 두고 메서드 이름을 `FillByDiscontinued` 하 고 `GetDiscontinuedProducts`합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-331">Leave both checkboxes checked and name the methods `FillByDiscontinued` and `GetDiscontinuedProducts`.</span></span> <span data-ttu-id="60f92-332">마침을 클릭하여 마법사를 완료합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-332">Click Finish to complete the wizard.</span></span>

<span data-ttu-id="60f92-333">[![메서드 이름 FillByDiscontinued 및 GetDiscontinuedProducts](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image39.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image38.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-333">[![Name the Methods FillByDiscontinued and GetDiscontinuedProducts](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image39.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image38.png)</span></span>

<span data-ttu-id="60f92-334">**그림 18**: 메서드 이름 `FillByDiscontinued` 및 `GetDiscontinuedProducts` ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image40.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-334">**Figure 18**: Name the Methods `FillByDiscontinued` and `GetDiscontinuedProducts` ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image40.png))</span></span>

<span data-ttu-id="60f92-335">이러한 단계를 반복 하 여 `GetProductsWithPriceLessThan` 관리 되는 저장 프로시저에 대 한 `ProductsTableAdapter`에서 `FillByPriceLessThan` 및 `GetProductsWithPriceLessThan` 라는 메서드를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-335">Repeat these steps to create methods named `FillByPriceLessThan` and `GetProductsWithPriceLessThan` in the `ProductsTableAdapter` for the `GetProductsWithPriceLessThan` managed stored procedure.</span></span>

<span data-ttu-id="60f92-336">그림 19에서는 `GetDiscontinuedProducts` 및 `GetProductsWithPriceLessThan` 관리 되는 저장 프로시저에 대 한 `ProductsTableAdapter`에 메서드를 추가한 후 데이터 집합 디자이너의 스크린샷을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-336">Figure 19 shows a screenshot of the DataSet Designer after adding the methods to the `ProductsTableAdapter` for the `GetDiscontinuedProducts` and `GetProductsWithPriceLessThan` managed stored procedures.</span></span>

<span data-ttu-id="60f92-337">[이 단계에서 추가 된 새 메서드를 포함 하는 제품 Stableadapter ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image42.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image41.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-337">[![The ProductsTableAdapter Includes the New Methods Added in this Step](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image42.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image41.png)</span></span>

<span data-ttu-id="60f92-338">**그림 19**:이 단계에서 추가 된 새 메서드를 포함 하는 `ProductsTableAdapter` ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image43.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-338">**Figure 19**: The `ProductsTableAdapter` Includes the New Methods Added in this Step ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image43.png))</span></span>

## <a name="step-7-adding-corresponding-methods-to-the-business-logic-layer"></a><span data-ttu-id="60f92-339">7 단계: 비즈니스 논리 계층에 해당 메서드 추가</span><span class="sxs-lookup"><span data-stu-id="60f92-339">Step 7: Adding Corresponding Methods to the Business Logic Layer</span></span>

<span data-ttu-id="60f92-340">4 단계와 5 단계에서 추가 된 관리 되는 저장 프로시저를 호출 하는 메서드를 포함 하도록 데이터 액세스 계층을 업데이트 했으므로 비즈니스 논리 계층에 해당 메서드를 추가 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-340">Now that we have updated the Data Access Layer to include methods for calling the managed stored procedures added in Steps 4 and 5, we need to add corresponding methods to the Business Logic Layer.</span></span> <span data-ttu-id="60f92-341">`ProductsBLLWithSprocs` 클래스에 다음 두 메서드를 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-341">Add the following two methods to the `ProductsBLLWithSprocs` class:</span></span>

[!code-csharp[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample7.cs)]

<span data-ttu-id="60f92-342">두 메서드는 모두 해당 DAL 메서드를 호출 하 고 `ProductsDataTable` 인스턴스를 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-342">Both methods simply call the corresponding DAL method and return the `ProductsDataTable` instance.</span></span> <span data-ttu-id="60f92-343">각 메서드 위의 `DataObjectMethodAttribute` 태그는 ObjectDataSource s 데이터 원본 구성 마법사의 선택 탭에 있는 드롭다운 목록에 이러한 메서드를 포함 시킵니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-343">The `DataObjectMethodAttribute` markup above each method causes these methods to be included in the drop-down list in the SELECT tab of the ObjectDataSource s Configure Data Source wizard.</span></span>

## <a name="step-8-invoking-the-managed-stored-procedures-from-the-presentation-layer"></a><span data-ttu-id="60f92-344">8 단계: 프레젠테이션 계층에서 관리 되는 저장 프로시저 호출</span><span class="sxs-lookup"><span data-stu-id="60f92-344">Step 8: Invoking the Managed Stored Procedures from the Presentation Layer</span></span>

<span data-ttu-id="60f92-345">비즈니스 논리 및 데이터 액세스 계층을 확장 하 여 `GetDiscontinuedProducts` 및 `GetProductsWithPriceLessThan` 관리 되는 저장 프로시저를 호출 하는 기능을 포함 하 여 이제 ASP.NET 페이지를 통해 이러한 저장 프로시저 결과를 표시할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-345">With the Business Logic and Data Access Layers augmented to include support for calling the `GetDiscontinuedProducts` and `GetProductsWithPriceLessThan` managed stored procedures, we can now display these stored procedures results through an ASP.NET page.</span></span>

<span data-ttu-id="60f92-346">`AdvancedDAL` 폴더에서 `ManagedFunctionsAndSprocs.aspx` 페이지를 열고 도구 상자에서 GridView를 디자이너로 끌어 옵니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-346">Open the `ManagedFunctionsAndSprocs.aspx` page in the `AdvancedDAL` folder and, from the Toolbox, drag a GridView onto the Designer.</span></span> <span data-ttu-id="60f92-347">GridView s `ID` 속성을 `DiscontinuedProducts`로 설정 하 고 스마트 태그에서 `DiscontinuedProductsDataSource`라는 새 ObjectDataSource에 바인딩합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-347">Set the GridView s `ID` property to `DiscontinuedProducts` and, from its smart tag, bind it to a new ObjectDataSource named `DiscontinuedProductsDataSource`.</span></span> <span data-ttu-id="60f92-348">`ProductsBLLWithSprocs` 클래스 s `GetDiscontinuedProducts` 메서드에서 데이터를 가져오도록 ObjectDataSource를 구성 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-348">Configure the ObjectDataSource to pull its data from the `ProductsBLLWithSprocs` class s `GetDiscontinuedProducts` method.</span></span>

<span data-ttu-id="60f92-349">[ProductsBLLWithSprocs 클래스를 사용 하도록 ObjectDataSource 구성 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image45.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image44.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-349">[![Configure the ObjectDataSource to Use the ProductsBLLWithSprocs Class](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image45.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image44.png)</span></span>

<span data-ttu-id="60f92-350">**그림 20**: `ProductsBLLWithSprocs` 클래스를 사용 하도록 ObjectDataSource 구성 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image46.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-350">**Figure 20**: Configure the ObjectDataSource to Use the `ProductsBLLWithSprocs` Class ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image46.png))</span></span>

<span data-ttu-id="60f92-351">[![선택 탭의 드롭다운 목록에서 GetDiscontinuedProducts 메서드를 선택 합니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image48.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image47.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-351">[![Choose the GetDiscontinuedProducts Method from the Drop-Down List in the SELECT Tab](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image48.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image47.png)</span></span>

<span data-ttu-id="60f92-352">**그림 21**: 선택 탭의 드롭다운 목록에서 `GetDiscontinuedProducts` 메서드 선택 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image49.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-352">**Figure 21**: Choose the `GetDiscontinuedProducts` Method from the Drop-Down List in the SELECT Tab ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image49.png))</span></span>

<span data-ttu-id="60f92-353">이 표는 제품 정보만 표시 하는 데 사용 되므로 업데이트, 삽입 및 삭제 탭의 드롭다운 목록을 (없음)으로 설정 하 고 마침을 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-353">Since this grid will be used to just display product information, set the drop-down lists in the UPDATE, INSERT, and DELETE tabs to (None) and then click Finish.</span></span>

<span data-ttu-id="60f92-354">마법사가 완료 되 면 Visual Studio는 `ProductsDataTable`의 각 데이터 필드에 대해 BoundField 또는 CheckBoxField를 자동으로 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-354">Upon completing the wizard, Visual Studio will automatically add a BoundField or CheckBoxField for each data field in the `ProductsDataTable`.</span></span> <span data-ttu-id="60f92-355">`ProductName` 및 `Discontinued`를 제외 하 고 이러한 모든 필드를 제거 합니다 .이 시점에서 GridView 및 ObjectDataSource s 선언적 태그가 다음과 같이 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-355">Take a moment to remove all of these fields except for `ProductName` and `Discontinued`, at which point your GridView and ObjectDataSource s declarative markup should look similar to the following:</span></span>

[!code-aspx[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample8.aspx)]

<span data-ttu-id="60f92-356">잠시 시간을 사용 하 여 브라우저를 통해이 페이지를 봅니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-356">Take a moment to view this page through a browser.</span></span> <span data-ttu-id="60f92-357">페이지를 방문 하면 ObjectDataSource `ProductsBLLWithSprocs` 클래스 `GetDiscontinuedProducts` 메서드를 호출 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-357">When the page is visited, the ObjectDataSource calls the `ProductsBLLWithSprocs` class s `GetDiscontinuedProducts` method.</span></span> <span data-ttu-id="60f92-358">7 단계에서 살펴본 것 처럼이 메서드는 `GetDiscontinuedProducts` 저장 프로시저를 호출 하는 DAL s `ProductsDataTable` 클래스 `GetDiscontinuedProducts` 메서드를 호출 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-358">As we saw in Step 7, this method calls down to the DAL s `ProductsDataTable` class s `GetDiscontinuedProducts` method, which invokes the `GetDiscontinuedProducts` stored procedure.</span></span> <span data-ttu-id="60f92-359">이 저장 프로시저는 관리 되는 저장 프로시저이 고 3 단계에서 만든 코드를 실행 하 여 단종 된 제품을 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-359">This stored procedure is a managed stored procedure and executes the code we created in Step 3, returning the discontinued products.</span></span>

<span data-ttu-id="60f92-360">관리 되는 저장 프로시저에서 반환 되는 결과는 DAL에 의해 `ProductsDataTable` 패키지 된 후 BLL로 반환 됩니다. 그러면 해당 값이 GridView에 바인딩되고 표시 되는 프레젠테이션 계층으로 반환 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-360">The results returned by the managed stored procedure are packaged up into a `ProductsDataTable` by the DAL and then returned to the BLL, which then returns them to the Presentation Layer where they are bound to the GridView and displayed.</span></span> <span data-ttu-id="60f92-361">예상 대로 그리드는 단종 된 제품을 나열 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-361">As expected, the grid lists those products that have been discontinued.</span></span>

<span data-ttu-id="60f92-362">[단종 된 제품이 나열 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image51.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image50.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-362">[![The Discontinued Products are Listed](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image51.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image50.png)</span></span>

<span data-ttu-id="60f92-363">**그림 22**: 단종 된 제품이 나열 됩니다 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image52.png)).</span><span class="sxs-lookup"><span data-stu-id="60f92-363">**Figure 22**: The Discontinued Products are Listed ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image52.png))</span></span>

<span data-ttu-id="60f92-364">추가 연습을 위해 텍스트 상자와 다른 GridView를 페이지에 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-364">For further practice, add a TextBox and another GridView to the page.</span></span> <span data-ttu-id="60f92-365">이 GridView에 `ProductsBLLWithSprocs` 클래스 `GetProductsWithPriceLessThan` 메서드를 호출 하 여 텍스트 상자에 입력 한 양보다 작은 제품을 표시 하도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-365">Have this GridView display the products less than the amount entered into the TextBox by calling the `ProductsBLLWithSprocs` class s `GetProductsWithPriceLessThan` method.</span></span>

## <a name="step-9-creating-and-calling-t-sql-udfs"></a><span data-ttu-id="60f92-366">9 단계: T-sql Udf 만들기 및 호출</span><span class="sxs-lookup"><span data-stu-id="60f92-366">Step 9: Creating and Calling T-SQL UDFs</span></span>

<span data-ttu-id="60f92-367">Udf (사용자 정의 함수)는 프로그래밍 언어의 함수 의미 체계를 긴밀 하 게 모방 하는 데이터베이스 개체입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-367">User-Defined Functions, or UDFs, are database objects the closely mimic the semantics of functions in programming languages.</span></span> <span data-ttu-id="60f92-368">의 C#함수와 마찬가지로 udf는 다양 한 입력 매개 변수를 포함 하 고 특정 형식의 값을 반환할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-368">Like a function in C#, UDFs can include a variable number of input parameters and return a value of a particular type.</span></span> <span data-ttu-id="60f92-369">UDF는 문자열, 정수 등의 스칼라 데이터 또는 테이블 형식 데이터를 반환할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-369">A UDF can return either scalar data - a string, an integer, and so forth - or tabular data.</span></span> <span data-ttu-id="60f92-370">에서는 스칼라 데이터 형식을 반환 하는 UDF에서 시작 하 여 두 가지 유형의 Udf를 간략히 살펴보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-370">Let s take a quick look at both types of UDFs, starting with a UDF that returns a scalar data type.</span></span>

<span data-ttu-id="60f92-371">다음 UDF는 특정 제품에 대 한 재고의 예상 값을 계산 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-371">The following UDF calculates the estimated value of the inventory for a particular product.</span></span> <span data-ttu-id="60f92-372">이를 위해 특정 제품에 대 한 `UnitPrice`, `UnitsInStock`및 `Discontinued` 값인 세 개의 입력 매개 변수를 가져와 `money`형식의 값을 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-372">It does so by taking in three input parameters - the `UnitPrice`, `UnitsInStock`, and `Discontinued` values for a particular product - and returns a value of type `money`.</span></span> <span data-ttu-id="60f92-373">`UnitPrice`에 `UnitsInStock`를 곱하여 재고의 예상 값을 계산 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-373">It computes the estimated value of the inventory by multiplying the `UnitPrice` by the `UnitsInStock`.</span></span> <span data-ttu-id="60f92-374">지원 되지 않는 항목의 경우이 값은 반입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-374">For discontinued items, this value is halved.</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample9.sql)]

<span data-ttu-id="60f92-375">이 UDF가 데이터베이스에 추가 되 면 프로그래밍 기능 폴더를 확장 하 고 함수를 확장 한 다음 스칼라 값 함수를 확장 하 여 Management Studio를 통해 찾을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-375">Once this UDF has been added to the database, it can be found through Management Studio by expanding the Programmability folder, then Functions, and then Scalar-value Functions.</span></span> <span data-ttu-id="60f92-376">다음과 같이 `SELECT` 쿼리에서 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-376">It can be used in a `SELECT` query like so:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample10.sql)]

<span data-ttu-id="60f92-377">`udf_ComputeInventoryValue` UDF를 Northwind 데이터베이스에 추가 했습니다. 그림 23은 Management Studio를 통해 볼 때 위의 `SELECT` 쿼리의 출력을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-377">I have added the `udf_ComputeInventoryValue` UDF to the Northwind database; Figure 23 shows the output of the above `SELECT` query when viewed through Management Studio.</span></span> <span data-ttu-id="60f92-378">또한 UDF는 개체 탐색기의 스칼라 값 함수 폴더 아래에 나열 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-378">Also note that the UDF is listed under the Scalar-value Functions folder in the Object Explorer.</span></span>

<span data-ttu-id="60f92-379">[각 제품의 인벤토리 값이 나열 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image54.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image53.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-379">[![Each Product s Inventory Values is Listed](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image54.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image53.png)</span></span>

<span data-ttu-id="60f92-380">**그림 23**: 각 제품의 인벤토리 값이 나열 됩니다 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image55.png)).</span><span class="sxs-lookup"><span data-stu-id="60f92-380">**Figure 23**: Each Product s Inventory Values is Listed ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image55.png))</span></span>

<span data-ttu-id="60f92-381">Udf는 표 형식 데이터를 반환할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-381">UDFs can also return tabular data.</span></span> <span data-ttu-id="60f92-382">예를 들어 특정 범주에 속하는 제품을 반환 하는 UDF를 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-382">For example, we can create a UDF that returns products that belong to a particular category:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample11.sql)]

<span data-ttu-id="60f92-383">`udf_GetProductsByCategoryID` UDF는 `@CategoryID` 입력 매개 변수를 허용 하 고 지정 된 `SELECT` 쿼리의 결과를 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-383">The `udf_GetProductsByCategoryID` UDF accepts a `@CategoryID` input parameter and returns the results of the specified `SELECT` query.</span></span> <span data-ttu-id="60f92-384">이 UDF를 만든 후에는 `SELECT` 쿼리의 `FROM` (또는 `JOIN`) 절에서이 UDF를 참조할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-384">Once created, this UDF can be referenced in the `FROM` (or `JOIN`) clause of a `SELECT` query.</span></span> <span data-ttu-id="60f92-385">다음 예에서는 각 음료에 대해 `ProductID`, `ProductName`및 `CategoryID` 값을 반환 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-385">The following example would return the `ProductID`, `ProductName`, and `CategoryID` values for each of the beverages.</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample12.sql)]

<span data-ttu-id="60f92-386">`udf_GetProductsByCategoryID` UDF를 Northwind 데이터베이스에 추가 했습니다. 그림 24는 Management Studio를 통해 볼 때 위의 `SELECT` 쿼리의 출력을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-386">I have added the `udf_GetProductsByCategoryID` UDF to the Northwind database; Figure 24 shows the output of the above `SELECT` query when viewed through Management Studio.</span></span> <span data-ttu-id="60f92-387">테이블 형식 데이터를 반환 하는 Udf는 개체 탐색기 s 테이블 값 함수 폴더에서 찾을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-387">UDFs that return tabular data can be found in the Object Explorer s Table-value Functions folder.</span></span>

<span data-ttu-id="60f92-388">[ProductID, ProductName 및 CategoryID가 각 음료에 대해 나열 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image57.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image56.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-388">[![The ProductID, ProductName, and CategoryID are Listed for Each Beverage](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image57.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image56.png)</span></span>

<span data-ttu-id="60f92-389">**그림 24**: 각 음료에 대해 `ProductID`, `ProductName`및 `CategoryID` 나열 됩니다 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image58.png)).</span><span class="sxs-lookup"><span data-stu-id="60f92-389">**Figure 24**: The `ProductID`, `ProductName`, and `CategoryID` are Listed for Each Beverage ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image58.png))</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-390">Udf를 만들고 사용 하는 방법에 대 한 자세한 내용은 [사용자 정의 함수 소개](http://www.sqlteam.com/item.asp?ItemID=1955)를 참조 하세요.</span><span class="sxs-lookup"><span data-stu-id="60f92-390">For more information on creating and using UDFs, check out [Intro to User-Defined Functions](http://www.sqlteam.com/item.asp?ItemID=1955).</span></span> <span data-ttu-id="60f92-391">또한 [사용자 정의 함수의 장점과 단점](http://www.samspublishing.com/articles/article.asp?p=31724&amp;rl=1)을 확인 하세요.</span><span class="sxs-lookup"><span data-stu-id="60f92-391">Also check out [Advantages and Drawbacks of User-Defined Functions](http://www.samspublishing.com/articles/article.asp?p=31724&amp;rl=1).</span></span>

## <a name="step-10-creating-a-managed-udf"></a><span data-ttu-id="60f92-392">10 단계: 관리 되는 UDF 만들기</span><span class="sxs-lookup"><span data-stu-id="60f92-392">Step 10: Creating a Managed UDF</span></span>

<span data-ttu-id="60f92-393">위의 예제에서 만든 `udf_ComputeInventoryValue` 및 `udf_GetProductsByCategoryID` Udf는 T-sql 데이터베이스 개체입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-393">The `udf_ComputeInventoryValue` and `udf_GetProductsByCategoryID` UDFs created in the above examples are T-SQL database objects.</span></span> <span data-ttu-id="60f92-394">SQL Server 2005은 3 단계와 5 단계에서 관리 되는 저장 프로시저와 마찬가지로 `ManagedDatabaseConstructs` 프로젝트에 추가할 수 있는 관리 되는 Udf도 지원 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-394">SQL Server 2005 also supports managed UDFs, which can be added to the `ManagedDatabaseConstructs` project just like the managed stored procedures from Steps 3 and 5.</span></span> <span data-ttu-id="60f92-395">이 단계에서는 관리 코드에서 `udf_ComputeInventoryValue` UDF를 구현 해 보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-395">For this step, let s implement the `udf_ComputeInventoryValue` UDF in managed code.</span></span>

<span data-ttu-id="60f92-396">`ManagedDatabaseConstructs` 프로젝트에 관리 되는 UDF를 추가 하려면 솔루션 탐색기에서 프로젝트 이름을 마우스 오른쪽 단추로 클릭 하 고 새 항목을 추가 하도록 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-396">To add a managed UDF to the `ManagedDatabaseConstructs` project, right-click on the project name in Solution Explorer and choose to Add a New Item.</span></span> <span data-ttu-id="60f92-397">새 항목 추가 대화 상자에서 사용자 정의 템플릿을 선택 하 고 새 UDF 파일의 이름을 `udf_ComputeInventoryValue_Managed.cs`합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-397">Select the User-Defined Template from the Add New Item dialog box and name the new UDF file `udf_ComputeInventoryValue_Managed.cs`.</span></span>

<span data-ttu-id="60f92-398">[새 관리 되는 UDF를 ManagedDatabaseConstructs 프로젝트에 추가 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image60.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image59.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-398">[![Add a New Managed UDF to the ManagedDatabaseConstructs Project](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image60.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image59.png)</span></span>

<span data-ttu-id="60f92-399">**그림 25**: `ManagedDatabaseConstructs` 프로젝트에 새로운 관리 되는 UDF 추가 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image61.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-399">**Figure 25**: Add a New Managed UDF to the `ManagedDatabaseConstructs` Project ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image61.png))</span></span>

<span data-ttu-id="60f92-400">사용자 정의 함수 템플릿은 이름이 클래스 파일 이름 (이 인스턴스의`udf_ComputeInventoryValue_Managed`)과 동일한 메서드를 사용 하 여 `UserDefinedFunctions` 이라는 `partial` 클래스를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-400">The User-Defined Function template creates a `partial` class named `UserDefinedFunctions` with a method whose name is the same as the class file s name (`udf_ComputeInventoryValue_Managed`, in this instance).</span></span> <span data-ttu-id="60f92-401">이 메서드는 메서드를 관리 되는 UDF로 플래그 하는 [`SqlFunction` 특성](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlfunctionattribute.aspx)을 사용 하 여 데코 레이트 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-401">This method is decorated using the [`SqlFunction` attribute](https://msdn.microsoft.com/library/microsoft.sqlserver.server.sqlfunctionattribute.aspx), which flags the method as a managed UDF.</span></span>

[!code-csharp[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample13.cs)]

<span data-ttu-id="60f92-402">`udf_ComputeInventoryValue` 메서드는 현재 [`SqlString` 개체](https://msdn.microsoft.com/library/system.data.sqltypes.sqlstring.aspx) 를 반환 하 고 입력 매개 변수를 허용 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-402">The `udf_ComputeInventoryValue` method currently returns a [`SqlString` object](https://msdn.microsoft.com/library/system.data.sqltypes.sqlstring.aspx) and does not accept any input parameters.</span></span> <span data-ttu-id="60f92-403">`UnitPrice`, `UnitsInStock`및 `Discontinued`의 입력 매개 변수 3 개를 허용 하 고 `SqlMoney` 개체를 반환 하도록 메서드 정의를 업데이트 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-403">We need to update the method definition so that it accepts three input parameters - `UnitPrice`, `UnitsInStock`, and `Discontinued` - and returns a `SqlMoney` object.</span></span> <span data-ttu-id="60f92-404">재고 값을 계산 하는 논리는 T-sql `udf_ComputeInventoryValue` UDF와 동일 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-404">The logic for calculating the inventory value is identical to that in the T-SQL `udf_ComputeInventoryValue` UDF.</span></span>

[!code-csharp[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample14.cs)]

<span data-ttu-id="60f92-405">UDF 메서드의 입력 매개 변수는 해당 하는 SQL 형식입니다. `UnitPrice` 필드의 `SqlMoney`, `UnitsInStock`[`SqlInt16`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlint16.aspx) ,`SqlBoolean`에 대 한 [`Discontinued`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlboolean.aspx) 입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-405">Note that the UDF method s input parameters are of their corresponding SQL types: `SqlMoney` for the `UnitPrice` field, [`SqlInt16`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlint16.aspx) for `UnitsInStock`, and [`SqlBoolean`](https://msdn.microsoft.com/library/system.data.sqltypes.sqlboolean.aspx) for `Discontinued`.</span></span> <span data-ttu-id="60f92-406">이러한 데이터 형식은 `Products` 테이블에 정의 된 유형을 반영 합니다. `UnitPrice` 열은 `money`형식이 고 `smallint`유형의 `UnitsInStock` 열 이며 `Discontinued` 유형의 `bit`열입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-406">These data types reflect the types defined in the `Products` table: the `UnitPrice` column is of type `money`, the `UnitsInStock` column of type `smallint`, and the `Discontinued` column of type `bit`.</span></span>

<span data-ttu-id="60f92-407">이 코드는 0 값이 할당 된 `inventoryValue` 라는 `SqlMoney` 인스턴스를 만들어 시작 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-407">The code starts by creating a `SqlMoney` instance named `inventoryValue` that is assigned a value of 0.</span></span> <span data-ttu-id="60f92-408">`Products` 테이블은 `UnitsInPrice` 및 `UnitsInStock` 열의 데이터베이스 `NULL` 값을 허용 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-408">The `Products` table allows for database `NULL` values in the `UnitsInPrice` and `UnitsInStock` columns.</span></span> <span data-ttu-id="60f92-409">따라서 이러한 값에 `SqlMoney` 개체 [`IsNull` 속성](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.isnull.aspx)을 통해 수행 되는 `NULL`가 포함 되어 있는지 먼저 확인 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-409">Therefore, we need to first check to see if these values contain `NULL` s, which we do through the `SqlMoney` object s [`IsNull` property](https://msdn.microsoft.com/library/system.data.sqltypes.sqlmoney.isnull.aspx).</span></span> <span data-ttu-id="60f92-410">`UnitPrice`와 `UnitsInStock` 모두`NULL` 아닌 값을 포함 하는 경우 `inventoryValue`를 두 제품으로 계산 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-410">If both `UnitPrice` and `UnitsInStock` contain non-`NULL` values, then we compute the `inventoryValue` to be the product of the two.</span></span> <span data-ttu-id="60f92-411">그런 다음 `Discontinued` true 이면 값을 halve 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-411">Then, if `Discontinued` is true, then we halve the value.</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-412">`SqlMoney` 개체를 사용 하는 경우 두 개의 `SqlMoney` 인스턴스만 함께 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-412">The `SqlMoney` object only allows two `SqlMoney` instances to be multiplied together.</span></span> <span data-ttu-id="60f92-413">`SqlMoney` 인스턴스를 리터럴 부동 소수점 숫자로 곱할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-413">It does not allow a `SqlMoney` instance to be multiplied by a literal floating-point number.</span></span> <span data-ttu-id="60f92-414">따라서 halve `inventoryValue` 하려면 값이 0.5 인 새 `SqlMoney` 인스턴스에 곱합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-414">Therefore, to halve `inventoryValue` we multiply it by a new `SqlMoney` instance that has the value 0.5.</span></span>

## <a name="step-11-deploying-the-managed-udf"></a><span data-ttu-id="60f92-415">11 단계: 관리 되는 UDF 배포</span><span class="sxs-lookup"><span data-stu-id="60f92-415">Step 11: Deploying the Managed UDF</span></span>

<span data-ttu-id="60f92-416">이제 관리 되는 UDF를 만들었으므로 Northwind 데이터베이스에 배포할 준비가 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-416">Now that the managed UDF has been created, we are ready to deploy it to the Northwind database.</span></span> <span data-ttu-id="60f92-417">4 단계에서 살펴본 것 처럼 SQL Server 프로젝트의 관리 되는 개체는 솔루션 탐색기에서 프로젝트 이름을 마우스 오른쪽 단추로 클릭 하 고 상황에 맞는 메뉴에서 배포 옵션을 선택 하 여 배포 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-417">As we saw in Step 4, the managed objects in a SQL Server Project are deployed by right-clicking on the project name in the Solution Explorer and choosing the Deploy option from the context menu.</span></span>

<span data-ttu-id="60f92-418">프로젝트를 배포한 후 SQL Server Management Studio로 돌아가서 스칼라 반환 함수 폴더를 새로 고칩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-418">Once you have deployed the project, return to SQL Server Management Studio and refresh the Scalar-valued Functions folder.</span></span> <span data-ttu-id="60f92-419">이제 두 개의 항목이 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-419">You should now see two entries:</span></span>

- <span data-ttu-id="60f92-420">`dbo.udf_ComputeInventoryValue`-9 단계에서 만든 T-sql UDF</span><span class="sxs-lookup"><span data-stu-id="60f92-420">`dbo.udf_ComputeInventoryValue` - the T-SQL UDF created in Step 9, and</span></span>
- <span data-ttu-id="60f92-421">`dbo.udf ComputeInventoryValue_Managed`-방금 배포 된 10 단계에서 만든 관리 되는 UDF입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-421">`dbo.udf ComputeInventoryValue_Managed` - the managed UDF created in Step 10 that was just deployed.</span></span>

<span data-ttu-id="60f92-422">이 관리 되는 UDF를 테스트 하려면 Management Studio 내에서 다음 쿼리를 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-422">To test this managed UDF, execute the following query from within Management Studio:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample15.sql)]

<span data-ttu-id="60f92-423">이 명령은 T-sql `udf_ComputeInventoryValue` UDF 대신 관리 되는 `udf ComputeInventoryValue_Managed` UDF를 사용 하지만 출력은 동일 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-423">This command uses the managed `udf ComputeInventoryValue_Managed` UDF instead of the T-SQL `udf_ComputeInventoryValue` UDF, but the output is the same.</span></span> <span data-ttu-id="60f92-424">그림 23을 참조 하 여 UDF의 출력에 대 한 스크린샷을 확인 하세요.</span><span class="sxs-lookup"><span data-stu-id="60f92-424">Refer back to Figure 23 to see a screenshot of the UDF s output.</span></span>

## <a name="step-12-debugging-the-managed-database-objects"></a><span data-ttu-id="60f92-425">12 단계: 관리 되는 데이터베이스 개체 디버깅</span><span class="sxs-lookup"><span data-stu-id="60f92-425">Step 12: Debugging the Managed Database Objects</span></span>

<span data-ttu-id="60f92-426">[저장 프로시저 디버깅](debugging-stored-procedures-cs.md) 자습서에서는 Visual Studio를 통한 SQL Server 디버깅을 위한 세 가지 옵션, 즉 직접 데이터베이스 디버깅, 응용 프로그램 디버깅 및 SQL Server 프로젝트의 디버깅에 대해 설명 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-426">In the [Debugging Stored Procedures](debugging-stored-procedures-cs.md) tutorial we discussed the three options for debugging SQL Server through Visual Studio: Direct Database Debugging, Application Debugging, and Debugging from a SQL Server Project.</span></span> <span data-ttu-id="60f92-427">관리 되는 데이터베이스 개체는 직접 데이터베이스 디버깅을 통해 디버그할 수 없지만 클라이언트 응용 프로그램에서 디버그 하 고 SQL Server 프로젝트에서 직접 디버그할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-427">Managed database objects cannot be debugged via Direct Database Debugging, but can be debugged from a client application and directly from the SQL Server Project.</span></span> <span data-ttu-id="60f92-428">그러나 디버깅이 작동 하려면 SQL Server 2005 데이터베이스에서 SQL/CLR 디버깅을 허용 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-428">In order for debugging to work, however, the SQL Server 2005 database must allow SQL/CLR debugging.</span></span> <span data-ttu-id="60f92-429">`ManagedDatabaseConstructs` 프로젝트를 처음 만들었을 때 SQL/CLR 디버깅을 사용 하도록 설정할지 묻는 메시지가 표시 됩니다 (2 단계에서 그림 6 참조).</span><span class="sxs-lookup"><span data-stu-id="60f92-429">Recall that when we first created the `ManagedDatabaseConstructs` project Visual Studio asked us whether we wanted to enable SQL/CLR debugging (see Figure 6 in Step 2).</span></span> <span data-ttu-id="60f92-430">이 설정은 서버 탐색기 창에서 데이터베이스를 마우스 오른쪽 단추로 클릭 하 여 수정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-430">This setting can be modified by right-clicking on the database from the Server Explorer window.</span></span>

![데이터베이스에서 SQL/CLR 디버깅을 허용 하는지 확인 합니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image62.png)

<span data-ttu-id="60f92-432">**그림 26**: 데이터베이스에서 SQL/CLR 디버깅을 허용 하는지 확인</span><span class="sxs-lookup"><span data-stu-id="60f92-432">**Figure 26**: Ensure that the Database Allows SQL/CLR Debugging</span></span>

<span data-ttu-id="60f92-433">`GetProductsWithPriceLessThan` 관리 되는 저장 프로시저를 디버깅 하려고 한다고 가정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-433">Imagine that we wanted to debug the `GetProductsWithPriceLessThan` managed stored procedure.</span></span> <span data-ttu-id="60f92-434">먼저 `GetProductsWithPriceLessThan` 메서드의 코드 내에서 중단점을 설정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-434">We would start by setting a breakpoint within the code of the `GetProductsWithPriceLessThan` method.</span></span>

<span data-ttu-id="60f92-435">[GetProductsWithPriceLessThan 메서드에 중단점을 설정 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image64.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image63.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-435">[![Set a Breakpoint in the GetProductsWithPriceLessThan Method](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image64.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image63.png)</span></span>

<span data-ttu-id="60f92-436">**그림 27**: `GetProductsWithPriceLessThan` 메서드에서 중단점 설정 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image65.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-436">**Figure 27**: Set a Breakpoint in the `GetProductsWithPriceLessThan` Method ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image65.png))</span></span>

<span data-ttu-id="60f92-437">먼저 SQL Server 프로젝트에서 관리 되는 데이터베이스 개체를 디버깅 하는 방법을 살펴보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-437">Let s first look at debugging the managed database objects from the SQL Server Project.</span></span> <span data-ttu-id="60f92-438">이 솔루션에는 두 개의 프로젝트가 포함 되어 있으므로 `ManagedDatabaseConstructs` SQL Server 프로젝트를 웹 사이트와 함께 사용 하 여 SQL Server 프로젝트에서 디버그 하기 위해 디버깅을 시작할 때 Visual Studio에서 `ManagedDatabaseConstructs` SQL Server 프로젝트를 시작 하도록 지시 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-438">Since our Solution includes two projects - the `ManagedDatabaseConstructs` SQL Server Project along with our website - in order to debug from the SQL Server Project we need to instruct Visual Studio to launch the `ManagedDatabaseConstructs` SQL Server Project when we start debugging.</span></span> <span data-ttu-id="60f92-439">솔루션 탐색기에서 `ManagedDatabaseConstructs` 프로젝트를 마우스 오른쪽 단추로 클릭 하 고 상황에 맞는 메뉴에서 시작 프로젝트로 설정 옵션을 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-439">Right-click the `ManagedDatabaseConstructs` project in Solution Explorer and choose the Set as StartUp Project option from the context menu.</span></span>

<span data-ttu-id="60f92-440">디버거에서 `ManagedDatabaseConstructs` 프로젝트를 시작 하면 `Test Scripts` 폴더에 있는 `Test.sql` 파일에서 SQL 문을 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-440">When the `ManagedDatabaseConstructs` project is launched from the debugger it executes the SQL statements in the `Test.sql` file, which is located in the `Test Scripts` folder.</span></span> <span data-ttu-id="60f92-441">예를 들어 `GetProductsWithPriceLessThan` 관리 되는 저장 프로시저를 테스트 하려면 기존 `Test.sql` 파일 콘텐츠를 다음 문으로 바꿉니다 .이 문은 `@CategoryID` 값 14.95를 전달 하 `GetProductsWithPriceLessThan` 관리 되는 저장 프로시저를 호출 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-441">For example, to test the `GetProductsWithPriceLessThan` managed stored procedure, replace the existing `Test.sql` file content with the following statement, which invokes the `GetProductsWithPriceLessThan` managed stored procedure passing in the `@CategoryID` value of 14.95:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample16.sql)]

<span data-ttu-id="60f92-442">위의 스크립트를 `Test.sql`에 입력 한 후 디버그 메뉴로 이동 하 고 디버깅 시작을 선택 하거나 도구 모음에서 F5 또는 녹색 재생 아이콘을 선택 하 여 디버깅을 시작 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-442">Once you ve entered the above script into `Test.sql`, start debugging by going to the Debug menu and choosing Start Debugging or by hitting F5 or the green play icon in the Toolbar.</span></span> <span data-ttu-id="60f92-443">이렇게 하면 솔루션 내에서 프로젝트가 작성 되 고, 관리 되는 데이터베이스 개체를 Northwind 데이터베이스에 배포한 후 `Test.sql` 스크립트가 실행 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-443">This will build the projects within the Solution, deploy the managed database objects to the Northwind database, and then execute the `Test.sql` script.</span></span> <span data-ttu-id="60f92-444">이 시점에서 중단점에 도달 하면 `GetProductsWithPriceLessThan` 메서드를 단계별로 실행 하 고 입력 매개 변수 값을 검사할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-444">At this point, the breakpoint will be hit and we can step through the `GetProductsWithPriceLessThan` method, examine the values of the input parameters, and so on.</span></span>

<span data-ttu-id="60f92-445">[GetProductsWithPriceLessThan 메서드의 중단점이 적중 된 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image67.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image66.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-445">[![The Breakpoint in the GetProductsWithPriceLessThan Method Was Hit](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image67.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image66.png)</span></span>

<span data-ttu-id="60f92-446">**그림 28**: `GetProductsWithPriceLessThan` 메서드의 중단점이 적중 되었습니다 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image68.png)).</span><span class="sxs-lookup"><span data-stu-id="60f92-446">**Figure 28**: The Breakpoint in the `GetProductsWithPriceLessThan` Method Was Hit ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image68.png))</span></span>

<span data-ttu-id="60f92-447">클라이언트 응용 프로그램을 통해 SQL database 개체를 디버깅 하려면 응용 프로그램 디버깅을 지원 하도록 데이터베이스를 구성 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-447">In order for a SQL database object to be debugged through a client application, it is imperative that the database be configured to support application debugging.</span></span> <span data-ttu-id="60f92-448">서버 탐색기에서 데이터베이스를 마우스 오른쪽 단추로 클릭 하 고 응용 프로그램 디버깅 옵션이 선택 되어 있는지 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-448">Right-click on the database in Server Explorer and ensure that the Application Debugging option is checked.</span></span> <span data-ttu-id="60f92-449">또한 SQL 디버거와 통합 하 고 연결 풀링을 사용 하지 않도록 설정 하려면 ASP.NET 응용 프로그램을 구성 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-449">Furthermore, we need to configure the ASP.NET application to integrate with the SQL Debugger and to disable connection pooling.</span></span> <span data-ttu-id="60f92-450">이러한 단계는 [저장 프로시저 디버깅](debugging-stored-procedures-cs.md) 자습서의 2 단계에 자세히 설명 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-450">These steps were discussed in detail in Step 2 of the [Debugging Stored Procedures](debugging-stored-procedures-cs.md) tutorial.</span></span>

<span data-ttu-id="60f92-451">ASP.NET 응용 프로그램 및 데이터베이스를 구성한 후에는 ASP.NET 웹 사이트를 시작 프로젝트로 설정 하 고 디버깅을 시작 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-451">Once you have configured the ASP.NET application and database, set the ASP.NET website as the startup project and start debugging.</span></span> <span data-ttu-id="60f92-452">중단점을 포함 하는 관리 되는 개체 중 하나를 호출 하는 페이지를 방문 하면 응용 프로그램이 중단 되 고 디버거로 제어가 중단 됩니다. 여기서 그림 28과 같이 코드를 단계별로 실행 하면 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-452">If you visit a page that calls one of the managed objects that has a breakpoint, the application will halt and control will be turned over to the debugger, where you can step through the code as shown in Figure 28.</span></span>

## <a name="step-13-manually-compiling-and-deploying-managed-database-objects"></a><span data-ttu-id="60f92-453">13 단계: 관리 되는 데이터베이스 개체 수동 컴파일 및 배포</span><span class="sxs-lookup"><span data-stu-id="60f92-453">Step 13: Manually Compiling and Deploying Managed Database Objects</span></span>

<span data-ttu-id="60f92-454">SQL Server 프로젝트를 사용 하면 관리 되는 데이터베이스 개체를 쉽게 만들고 컴파일하고 배포할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-454">SQL Server Projects make it easy to create, compile, and deploy managed database objects.</span></span> <span data-ttu-id="60f92-455">그러나 SQL Server 프로젝트는 Visual Studio Professional 및 Team Systems edition 에서만 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-455">Unfortunately, SQL Server Projects are only available in the Professional and Team Systems editions of Visual Studio.</span></span> <span data-ttu-id="60f92-456">Visual Studio의 visual Web Developer 또는 Standard Edition을 사용 하 고 관리 되는 데이터베이스 개체를 사용 하려는 경우 수동으로 만들고 배포 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-456">If you are using Visual Web Developer or the Standard Edition of Visual Studio and want to use managed database objects, you will need to manually create and deploy them.</span></span> <span data-ttu-id="60f92-457">여기에는 4 단계가 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-457">This involves four steps:</span></span>

1. <span data-ttu-id="60f92-458">관리 되는 데이터베이스 개체에 대 한 소스 코드를 포함 하는 파일을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-458">Create a file that contains the source code for the managed database object,</span></span>
2. <span data-ttu-id="60f92-459">개체를 어셈블리로 컴파일합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-459">Compile the object into an assembly,</span></span>
3. <span data-ttu-id="60f92-460">어셈블리를 SQL Server 2005 데이터베이스에 등록 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-460">Register the assembly with the SQL Server 2005 database, and</span></span>
4. <span data-ttu-id="60f92-461">어셈블리에서 적절 한 메서드를 가리키는 SQL Server 데이터베이스 개체를 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-461">Create a database object in SQL Server that points to the appropriate method in the assembly.</span></span>

<span data-ttu-id="60f92-462">이러한 작업을 설명 하기 위해에서 `UnitPrice` 지정 된 값 보다 큰 제품을 반환 하는 새 관리 되는 저장 프로시저를 만들어 보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-462">To illustrate these tasks, let s create a new managed stored procedure that returns those products whose `UnitPrice` is greater than a specified value.</span></span> <span data-ttu-id="60f92-463">`GetProductsWithPriceGreaterThan.cs` 이라는 컴퓨터에 새 파일을 만들고 다음 코드를 파일에 입력 합니다. Visual Studio, 메모장 또는 텍스트 편집기를 사용 하 여이 작업을 수행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-463">Create a new file on your computer named `GetProductsWithPriceGreaterThan.cs` and enter the following code into the file (you can use Visual Studio, Notepad, or any text editor to accomplish this):</span></span>

[!code-csharp[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample17.cs)]

<span data-ttu-id="60f92-464">이 코드는 5 단계에서 만든 `GetProductsWithPriceLessThan` 메서드의 코드와 거의 동일 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-464">This code is nearly identical to that of the `GetProductsWithPriceLessThan` method created in Step 5.</span></span> <span data-ttu-id="60f92-465">유일한 차이점은 메서드 이름, `WHERE` 절 및 쿼리에 사용 되는 매개 변수 이름입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-465">The only differences are the method names, the `WHERE` clause, and the parameter name used in the query.</span></span> <span data-ttu-id="60f92-466">`GetProductsWithPriceLessThan` 메서드로 돌아가서 `WHERE` 절을 읽습니다. `WHERE UnitPrice < @MaxPrice`.</span><span class="sxs-lookup"><span data-stu-id="60f92-466">Back in the `GetProductsWithPriceLessThan` method, the `WHERE` clause read: `WHERE UnitPrice < @MaxPrice`.</span></span> <span data-ttu-id="60f92-467">`GetProductsWithPriceGreaterThan`에서 `WHERE UnitPrice > @MinPrice`를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-467">Here, in `GetProductsWithPriceGreaterThan`, we use: `WHERE UnitPrice > @MinPrice` .</span></span>

<span data-ttu-id="60f92-468">이제이 클래스를 어셈블리로 컴파일해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-468">We now need to compile this class into an assembly.</span></span> <span data-ttu-id="60f92-469">명령줄에서 `GetProductsWithPriceGreaterThan.cs` 파일을 저장 한 디렉터리로 이동 하 고 C# 컴파일러 (`csc.exe`)를 사용 하 여 클래스 파일을 어셈블리로 컴파일합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-469">From the command line, navigate to the directory where you saved the `GetProductsWithPriceGreaterThan.cs` file and use the C# compiler (`csc.exe`) to compile the class file into an assembly:</span></span>

[!code-console[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample18.cmd)]

<span data-ttu-id="60f92-470">`csc.exe`를 포함 하는 폴더가 시스템에 `PATH`있지 않은 경우 다음과 같이 경로, `%WINDOWS%\Microsoft.NET\Framework\version\`를 모두 참조 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-470">If the folder containing `csc.exe` in not in the system s `PATH`, you will have to fully reference its path, `%WINDOWS%\Microsoft.NET\Framework\version\`, like so:</span></span>

[!code-console[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample19.cmd)]

<span data-ttu-id="60f92-471">[GetProductsWithPriceGreaterThan.cs을 어셈블리로 컴파일 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image70.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image69.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-471">[![Compile GetProductsWithPriceGreaterThan.cs Into an Assembly](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image70.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image69.png)</span></span>

<span data-ttu-id="60f92-472">**그림 29**: 어셈블리에 `GetProductsWithPriceGreaterThan.cs` 컴파일 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image71.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-472">**Figure 29**: Compile `GetProductsWithPriceGreaterThan.cs` Into an Assembly ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image71.png))</span></span>

<span data-ttu-id="60f92-473">`/t` 플래그는 C# 클래스 파일을 실행 파일이 아닌 DLL로 컴파일하도록 지정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-473">The `/t` flag specifies that the C# class file should be compiled into a DLL (rather than an executable).</span></span> <span data-ttu-id="60f92-474">`/out` 플래그는 결과 어셈블리의 이름을 지정 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-474">The `/out` flag specifies the name of the resulting assembly.</span></span>

> [!NOTE]
> <span data-ttu-id="60f92-475">명령줄에서 `GetProductsWithPriceGreaterThan.cs` 클래스 파일을 컴파일하는 대신 [ C# visual Express edition](https://msdn.microsoft.com/vstudio/express/visualcsharp/) 을 사용 하거나 visual Studio Standard Edition에서 별도의 클래스 라이브러리 프로젝트를 만들 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-475">Rather than compiling the `GetProductsWithPriceGreaterThan.cs` class file from the command line you could alternatively use [Visual C# Express Edition](https://msdn.microsoft.com/vstudio/express/visualcsharp/) or create a separate Class Library project in Visual Studio Standard Edition.</span></span> <span data-ttu-id="60f92-476">S ren Jacob Lauritsen는 `GetProductsWithPriceGreaterThan` 저장 프로시저에 대 C# 한 코드 및 3, 5, 10 단계에서 만든 두 개의 관리 되는 저장 프로시저 및 UDF에 대 한 코드를 포함 하는 Visual Express Edition 프로젝트를 제공 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-476">S ren Jacob Lauritsen has kindly provided such a Visual C# Express Edition project with code for the `GetProductsWithPriceGreaterThan` stored procedure and the two managed stored procedures and UDF created in Steps 3, 5, and 10.</span></span> <span data-ttu-id="60f92-477">S ren s 프로젝트에는 해당 데이터베이스 개체를 추가 하는 데 필요한 T-sql 명령도 포함 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-477">S ren s project also includes the T-SQL commands needed to add the corresponding database objects.</span></span>

<span data-ttu-id="60f92-478">코드를 어셈블리로 컴파일하면 어셈블리를 SQL Server 2005 데이터베이스에 등록할 준비가 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-478">With the code compiled into an assembly, we are ready to register the assembly within the SQL Server 2005 database.</span></span> <span data-ttu-id="60f92-479">이는 명령 `CREATE ASSEMBLY`를 사용 하거나 SQL Server Management Studio를 통해 T-sql을 통해 수행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-479">This can be performed through T-SQL, using the command `CREATE ASSEMBLY`, or through SQL Server Management Studio.</span></span> <span data-ttu-id="60f92-480">Management Studio 사용에 중점을 둡니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-480">Let s focus on using Management Studio.</span></span>

<span data-ttu-id="60f92-481">Management Studio에서 Northwind 데이터베이스의 프로그래밍 기능 폴더를 확장 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-481">From Management Studio, expand the Programmability folder in the Northwind database.</span></span> <span data-ttu-id="60f92-482">하위 폴더 중 하나는 어셈블리입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-482">One of its subfolder is Assemblies.</span></span> <span data-ttu-id="60f92-483">데이터베이스에 새 어셈블리를 수동으로 추가 하려면 어셈블리 폴더를 마우스 오른쪽 단추로 클릭 하 고 상황에 맞는 메뉴에서 새 어셈블리를 선택 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-483">To manually add a new Assembly to the database, right-click on the Assemblies folder and choose New Assembly from the context menu.</span></span> <span data-ttu-id="60f92-484">그러면 새 어셈블리 대화 상자가 표시 됩니다 (그림 30 참조).</span><span class="sxs-lookup"><span data-stu-id="60f92-484">This displays the New Assembly dialog box (see Figure 30).</span></span> <span data-ttu-id="60f92-485">찾아보기 단추를 클릭 하 고 방금 컴파일한 `ManuallyCreatedDBObjects.dll` 어셈블리를 선택한 다음 확인을 클릭 하 여 어셈블리를 데이터베이스에 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-485">Click on the Browse button, select the `ManuallyCreatedDBObjects.dll` assembly we just compiled, and then click OK to add the Assembly to the database.</span></span> <span data-ttu-id="60f92-486">개체 탐색기에 `ManuallyCreatedDBObjects.dll` 어셈블리가 표시 되어서는 안 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-486">You should not see the `ManuallyCreatedDBObjects.dll` assembly in the Object Explorer.</span></span>

<span data-ttu-id="60f92-487">[ManuallyCreatedDBObjects 어셈블리를 데이터베이스에 추가 ![](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image73.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image72.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-487">[![Add the ManuallyCreatedDBObjects.dll Assembly to the Database](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image73.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image72.png)</span></span>

<span data-ttu-id="60f92-488">**그림 30**: 데이터베이스에 `ManuallyCreatedDBObjects.dll` 어셈블리 추가 ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image74.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-488">**Figure 30**: Add the `ManuallyCreatedDBObjects.dll` Assembly to the Database ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image74.png))</span></span>

![ManuallyCreatedDBObjects는 개체 탐색기에 나열 되어 있습니다.](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image75.png)

<span data-ttu-id="60f92-490">**그림 31**: `ManuallyCreatedDBObjects.dll` 개체 탐색기에 나열 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-490">**Figure 31**: The `ManuallyCreatedDBObjects.dll` is Listed in the Object Explorer</span></span>

<span data-ttu-id="60f92-491">Northwind 데이터베이스에 어셈블리를 추가 하는 동안 어셈블리의 `GetProductsWithPriceGreaterThan` 메서드와 저장 프로시저를 연결 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-491">While we have added the assembly to the Northwind database, we have yet to associate a stored procedure with the `GetProductsWithPriceGreaterThan` method in the assembly.</span></span> <span data-ttu-id="60f92-492">이 작업을 수행 하려면 새 쿼리 창을 열고 다음 스크립트를 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-492">To accomplish this, open a new query window and execute the following script:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample20.sql)]

<span data-ttu-id="60f92-493">그러면 `GetProductsWithPriceGreaterThan` 라는 Northwind 데이터베이스에 새 저장 프로시저가 생성 되 고이를 관리 되는 메서드 `GetProductsWithPriceGreaterThan`에 연결 합니다 .이는 어셈블리 `ManuallyCreatedDBObjects`의 `StoredProcedures`클래스에 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-493">This creates a new stored procedure in the Northwind database named `GetProductsWithPriceGreaterThan` and associates it with the managed method `GetProductsWithPriceGreaterThan` (which is in the class `StoredProcedures`, which is in the assembly `ManuallyCreatedDBObjects`).</span></span>

<span data-ttu-id="60f92-494">위의 스크립트를 실행 한 후 개체 탐색기에서 저장 프로시저 폴더를 새로 고칩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-494">After executing the above script, refresh the Stored Procedures folder in the Object Explorer.</span></span> <span data-ttu-id="60f92-495">옆에 잠금 아이콘이 있는 `GetProductsWithPriceGreaterThan` 새 저장 프로시저 항목이 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-495">You should see a new stored procedure entry - `GetProductsWithPriceGreaterThan` - which has a lock icon next to it.</span></span> <span data-ttu-id="60f92-496">이 저장 프로시저를 테스트 하려면 쿼리 창에서 다음 스크립트를 입력 하 고 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-496">To test this stored procedure, enter and execute the following script in the query window:</span></span>

[!code-sql[Main](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/samples/sample21.sql)]

<span data-ttu-id="60f92-497">그림 32에 나와 있는 것 처럼 위의 명령은 `UnitPrice` $24.95 보다 큰 제품에 대 한 정보를 표시 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-497">As Figure 32 shows, the above command displays information for those products with a `UnitPrice` greater than $24.95.</span></span>

<span data-ttu-id="60f92-498">[ManuallyCreatedDBObjects이에 나열 ![개체 탐색기](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image77.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image76.png)</span><span class="sxs-lookup"><span data-stu-id="60f92-498">[![The ManuallyCreatedDBObjects.dll is Listed in the Object Explorer](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image77.png)](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image76.png)</span></span>

<span data-ttu-id="60f92-499">**그림 32**: 개체 탐색기에 나열 된 `ManuallyCreatedDBObjects.dll` ([전체 크기 이미지를 보려면 클릭](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image78.png))</span><span class="sxs-lookup"><span data-stu-id="60f92-499">**Figure 32**: The `ManuallyCreatedDBObjects.dll` is Listed in the Object Explorer ([Click to view full-size image](creating-stored-procedures-and-user-defined-functions-with-managed-code-cs/_static/image78.png))</span></span>

## <a name="summary"></a><span data-ttu-id="60f92-500">요약</span><span class="sxs-lookup"><span data-stu-id="60f92-500">Summary</span></span>

<span data-ttu-id="60f92-501">Microsoft SQL Server 2005는 관리 코드를 사용 하 여 데이터베이스 개체를 만들 수 있도록 하는 CLR (공용 언어 런타임)과의 통합을 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-501">Microsoft SQL Server 2005 provides integration with the Common Language Runtime (CLR), which allows database objects to be created using managed code.</span></span> <span data-ttu-id="60f92-502">이전에는 T-sql을 사용 해야만 이러한 데이터베이스 개체를 만들 수 있었지만 이제와 같은 C#.net 프로그래밍 언어를 사용 하 여 이러한 개체를 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-502">Previously, these database objects could only be created using T-SQL, but now we can create these objects using .NET programming languages like C#.</span></span> <span data-ttu-id="60f92-503">이 자습서에서는 두 개의 관리 되는 저장 프로시저와 관리 되는 사용자 정의 함수를 만들었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-503">In this tutorial we created two managed stored procedures and a managed User-Defined Function.</span></span>

<span data-ttu-id="60f92-504">Visual Studio s SQL Server 프로젝트 형식은 관리 되는 데이터베이스 개체의 생성, 컴파일 및 배포를 용이 하 게 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-504">Visual Studio s SQL Server Project type facilitates creating, compiling, and deploying managed database objects.</span></span> <span data-ttu-id="60f92-505">또한 풍부한 디버깅 지원 기능을 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-505">Moreover, it offers rich debugging support.</span></span> <span data-ttu-id="60f92-506">그러나 SQL Server 프로젝트 형식은 Visual Studio Professional 및 Team Systems edition 에서만 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-506">However, SQL Server Project types are only available in the Professional and Team Systems editions of Visual Studio.</span></span> <span data-ttu-id="60f92-507">Visual Web Developer 또는 Standard Edition For Visual Studio를 사용 하는 경우 13 단계에서 살펴본 것 처럼 생성, 컴파일 및 배포 단계를 수동으로 수행 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-507">For those using Visual Web Developer or the Standard Edition of Visual Studio, the creation, compilation, and deployment steps must be performed manually, as we saw in Step 13.</span></span>

<span data-ttu-id="60f92-508">행복 한 프로그래밍</span><span class="sxs-lookup"><span data-stu-id="60f92-508">Happy Programming!</span></span>

## <a name="further-reading"></a><span data-ttu-id="60f92-509">추가 정보</span><span class="sxs-lookup"><span data-stu-id="60f92-509">Further Reading</span></span>

<span data-ttu-id="60f92-510">이 자습서에서 설명 하는 항목에 대 한 자세한 내용은 다음 리소스를 참조 하세요.</span><span class="sxs-lookup"><span data-stu-id="60f92-510">For more information on the topics discussed in this tutorial, refer to the following resources:</span></span>

- [<span data-ttu-id="60f92-511">사용자 정의 함수의 장점 및 단점</span><span class="sxs-lookup"><span data-stu-id="60f92-511">Advantages and Drawbacks of User-Defined Functions</span></span>](http://www.samspublishing.com/articles/article.asp?p=31724&amp;rl=1)
- [<span data-ttu-id="60f92-512">관리 코드에서 SQL Server 2005 개체 만들기</span><span class="sxs-lookup"><span data-stu-id="60f92-512">Creating SQL Server 2005 Objects in Managed Code</span></span>](https://channel9.msdn.com/Showpost.aspx?postid=142413)
- [<span data-ttu-id="60f92-513">SQL Server 2005에서 관리 코드를 사용 하 여 트리거 만들기</span><span class="sxs-lookup"><span data-stu-id="60f92-513">Creating Triggers Using Managed Code in SQL Server 2005</span></span>](http://www.15seconds.com/issue/041006.htm)
- <span data-ttu-id="60f92-514">[방법: CLR SQL Server 저장 프로시저 만들기 및 실행](https://msdn.microsoft.com/library/5czye81z(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="60f92-514">[How To: Create and Run a CLR SQL Server Stored Procedure](https://msdn.microsoft.com/library/5czye81z(VS.80).aspx)</span></span>
- <span data-ttu-id="60f92-515">[방법: CLR SQL Server 사용자 정의 함수 만들기 및 실행](https://msdn.microsoft.com/library/w2kae45k(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="60f92-515">[How To: Create and Run a CLR SQL Server User-Defined Function](https://msdn.microsoft.com/library/w2kae45k(VS.80).aspx)</span></span>
- <span data-ttu-id="60f92-516">[방법: SQL 개체를 실행 하는 `Test.sql` 스크립트 편집](https://msdn.microsoft.com/library/ms233682(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="60f92-516">[How To: Edit the `Test.sql` Script to Run SQL Objects](https://msdn.microsoft.com/library/ms233682(VS.80).aspx)</span></span>
- [<span data-ttu-id="60f92-517">사용자 정의 함수 소개</span><span class="sxs-lookup"><span data-stu-id="60f92-517">Intro to User Defined Functions</span></span>](http://www.sqlteam.com/item.asp?ItemID=1955)
- [<span data-ttu-id="60f92-518">관리 코드 및 SQL Server 2005 (비디오)</span><span class="sxs-lookup"><span data-stu-id="60f92-518">Managed Code and SQL Server 2005 (Video)</span></span>](https://channel9.msdn.com/Showpost.aspx?postid=142413)
- <span data-ttu-id="60f92-519">[Transact-sql 참조](https://msdn.microsoft.com/library/aa299742(SQL.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="60f92-519">[Transact-SQL Reference](https://msdn.microsoft.com/library/aa299742(SQL.80).aspx)</span></span>
- <span data-ttu-id="60f92-520">[연습: 관리 코드에서 저장 프로시저 만들기](https://msdn.microsoft.com/library/zxsa8hkf(VS.80).aspx)</span><span class="sxs-lookup"><span data-stu-id="60f92-520">[Walkthrough: Creating a Stored Procedure in Managed Code](https://msdn.microsoft.com/library/zxsa8hkf(VS.80).aspx)</span></span>

## <a name="about-the-author"></a><span data-ttu-id="60f92-521">작성자 정보</span><span class="sxs-lookup"><span data-stu-id="60f92-521">About the Author</span></span>

<span data-ttu-id="60f92-522">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml)(7 개의 ASP/ASP. NET books 및 [4GuysFromRolla.com](http://www.4guysfromrolla.com)창립자)은 1998부터 Microsoft 웹 기술을 사용 하 여 작업 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-522">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), author of seven ASP/ASP.NET books and founder of [4GuysFromRolla.com](http://www.4guysfromrolla.com), has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="60f92-523">Scott은 독립 컨설턴트, 강사 및 기록기로 작동 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-523">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="60f92-524">최신 책은 [*24 시간 이내에 ASP.NET 2.0을 sams teach yourself*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco)것입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-524">His latest book is [*Sams Teach Yourself ASP.NET 2.0 in 24 Hours*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span></span> <span data-ttu-id="60f92-525">mitchell@4GuysFromRolla.com에 도달할 수 있습니다 [.](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="60f92-525">He can be reached at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span> <span data-ttu-id="60f92-526">또는 블로그를 통해 [http://ScottOnWriting.NET](http://ScottOnWriting.NET)에서 찾을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-526">or via his blog, which can be found at [http://ScottOnWriting.NET](http://ScottOnWriting.NET).</span></span>

## <a name="special-thanks-to"></a><span data-ttu-id="60f92-527">특별히 감사 합니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-527">Special Thanks To</span></span>

<span data-ttu-id="60f92-528">이 자습서 시리즈는 많은 유용한 검토자가 검토 했습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-528">This tutorial series was reviewed by many helpful reviewers.</span></span> <span data-ttu-id="60f92-529">이 자습서의 리드 검토자는 S ren Jacob Lauritsen입니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-529">Lead reviewer for this tutorial was S ren Jacob Lauritsen.</span></span> <span data-ttu-id="60f92-530">이 문서를 검토 하는 것 외에도, S ren C# 는 관리 되는 데이터베이스 개체를 수동으로 컴파일하기 위해이 문서에 포함 된 Visual Express Edition 프로젝트를 만들었습니다.</span><span class="sxs-lookup"><span data-stu-id="60f92-530">In addition to reviewing this article, S ren also created the Visual C# Express Edition project included in this article s download for manually compiling the managed database objects.</span></span> <span data-ttu-id="60f92-531">예정 된 MSDN 문서를 검토 하는 데 관심이 있나요?</span><span class="sxs-lookup"><span data-stu-id="60f92-531">Interested in reviewing my upcoming MSDN articles?</span></span> <span data-ttu-id="60f92-532">그렇다면mitchell@4GuysFromRolla.com에서 줄을 삭제 [합니다.](mailto:mitchell@4GuysFromRolla.com)</span><span class="sxs-lookup"><span data-stu-id="60f92-532">If so, drop me a line at [mitchell@4GuysFromRolla.com.](mailto:mitchell@4GuysFromRolla.com)</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="60f92-533">[이전](debugging-stored-procedures-cs.md)
> [다음](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-vb.md)</span><span class="sxs-lookup"><span data-stu-id="60f92-533">[Previous](debugging-stored-procedures-cs.md)
[Next](creating-new-stored-procedures-for-the-typed-dataset-s-tableadapters-vb.md)</span></span>
