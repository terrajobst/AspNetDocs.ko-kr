---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
title: ASP.NET 웹 페이지 데이터 표시 소개 | Microsoft Docs
author: Rick-Anderson
description: 이 자습서에서는 WebMatrix에서 데이터베이스를 만드는 방법과 ASP.NET 웹 페이지 (Razor)를 사용할 때 페이지에 데이터베이스 데이터를 표시 하는 방법을 보여 줍니다. Y ...를 가정 합니다.
ms.author: riande
ms.date: 05/28/2015
ms.assetid: b3a006a0-3ea2-4d45-b833-e20e3a3c0a1a
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data
msc.type: authoredcontent
ms.openlocfilehash: 9e665ca8dd064c23a8b8bd3593014969d0c3da48
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 03/06/2020
ms.locfileid: "78421487"
---
# <a name="introducing-aspnet-web-pages---displaying-data"></a>ASP.NET 웹 페이지 데이터 표시 소개

만든 사람 [Tom FitzMacken](https://github.com/tfitzmac)

> 이 자습서에서는 WebMatrix에서 데이터베이스를 만드는 방법과 ASP.NET 웹 페이지 (Razor)를 사용할 때 페이지에 데이터베이스 데이터를 표시 하는 방법을 보여 줍니다. [ASP.NET 웹 페이지 프로그래밍에 대 한 소개를](../introducing-razor-syntax-c.md)통해 시리즈를 완료 했다고 가정 합니다.
> 
> 학습할 내용:
> 
> - WebMatrix 도구를 사용 하 여 데이터베이스 및 데이터베이스 테이블을 만드는 방법
> - WebMatrix 도구를 사용 하 여 데이터베이스에 데이터를 추가 하는 방법입니다.
> - 페이지의 데이터베이스에서 데이터를 표시 하는 방법
> - ASP.NET 웹 페이지에서 SQL 명령을 실행 하는 방법
> - `WebGrid` 도우미를 사용자 지정 하 여 데이터 표시를 변경 하 고 페이징 및 정렬을 추가 하는 방법입니다.
>   
> 
> 설명 하는 기능/기술:
> 
> - WebMatrix 데이터베이스 도구.
> - 도우미를 `WebGrid` 합니다.

## <a name="what-youll-build"></a>만들 내용

이전 자습서에서는 Razor 구문 및 도우미에 대 한 기본 사항에 ASP.NET 웹 페이지 (*cshtml* 파일)를 도입 했습니다. 이 자습서에서는 나머지 시리즈에 사용할 실제 웹 응용 프로그램을 만들기 시작 합니다. 앱은 영화에 대 한 정보를 보고 추가, 변경 및 삭제할 수 있는 간단한 영화 응용 프로그램입니다.

이 자습서를 완료 하면이 페이지와 같은 영화 목록을 볼 수 있습니다.

![CSS 클래스 이름으로 설정 된 매개 변수를 포함 하는 WebGrid 표시](displaying-data/_static/image1.png)

하지만 시작 하려면 데이터베이스를 만들어야 합니다.

## <a name="a-very-brief-introduction-to-databases"></a>데이터베이스에 대 한 간략 한 소개

이 자습서에서는 데이터베이스에 대 한 briefest 소개만 제공 합니다. 데이터베이스 환경이 있는 경우이 짧은 섹션을 건너뛸 수 있습니다.

데이터베이스에는 정보 &mdash; (예: 고객, 주문, 공급 업체 테이블 또는 학생, 교사, 수업 및 학점)를 포함 하는 테이블이 하나 이상 포함 되어 있습니다. 구조적으로 데이터베이스 테이블은 스프레드시트와 비슷합니다. 일반적인 주소록을 상상해 보세요. 주소록의 각 항목에 대해 (즉, 각 사람에 대 한) 이름, 성, 주소, 전자 메일 주소 및 전화 번호와 같은 여러 정보를 사용할 수 있습니다.

![간단한 표 형태의 샘플 데이터베이스 테이블](displaying-data/_static/image2.png)

(행을 *레코드*라고도 하며 열을 *필드*라고도 합니다.)

대부분의 데이터베이스 테이블에 대해 테이블에는 고객 번호, 계좌 번호 등과 같은 고유한 값을 포함 하는 열이 있어야 합니다. 이 값을 테이블의 *기본 키*라고 하며,이 값을 사용 하 여 테이블의 각 행을 식별 합니다. 이 예에서 ID 열은 이전 예제에 표시 된 주소록의 기본 키입니다.

웹 응용 프로그램에서 수행 하는 작업의 대부분은 데이터베이스에서 정보를 읽고 페이지에 표시 하는 것으로 구성 됩니다. 또한 사용자 로부터 정보를 수집 하 여 데이터베이스에 추가 하거나 이미 데이터베이스에 있는 레코드를 수정 하는 경우가 많습니다. (이 자습서 집합의 과정에서 이러한 모든 작업을 다룹니다.)

데이터베이스 작업은 있으므로 매우 복잡할 수 있으며 전문 지식이 필요할 수 있습니다. 그러나이 자습서에서는 기본 개념을 이해 해야 합니다 .이 개념은 모든 사용자에 게 설명 됩니다.

## <a name="creating-a-database"></a>데이터베이스 만들기

WebMatrix에는 데이터베이스를 쉽게 만들고 데이터베이스에서 테이블을 만들 수 있도록 하는 도구가 포함 되어 있습니다. 데이터베이스의 구조를 데이터베이스의 *스키마*라고 합니다. 이 자습서를 설정 하는 경우 동영상 &mdash; 하나의 테이블만 있는 데이터베이스를 만듭니다.

아직 수행 하지 않은 경우 WebMatrix를 열고 이전 자습서에서 만든 WebPagesMovies 사이트를 엽니다.

왼쪽 창에서 **데이터베이스** 작업 영역을 클릭 합니다.

![WebMatrix 데이터베이스 작업 영역 탭](displaying-data/_static/image3.png)

리본은 데이터베이스 관련 태스크를 표시 하도록 변경 됩니다. 리본에서 **새 데이터베이스**를 클릭 합니다.

![WebMatrix 리본의 ' 새 데이터베이스 ' 단추](displaying-data/_static/image4.png)

WebMatrix는 사이트 &mdash; *webstel.sdf*와 동일한 이름을 가진 SQL Server CE 데이터베이스 ( *.sdf* 파일)를 만듭니다. 여기서는이 작업을 수행 하지 않지만 확장명이 *.sdf* 인 경우 파일 이름을 원하는 이름으로 바꿀 수 있습니다.

## <a name="creating-a-table"></a>테이블 만들기

리본에서 **새 테이블**을 클릭 합니다. WebMatrix 새 탭에서 테이블 디자이너를 엽니다. 새 테이블 옵션을 사용할 수 없는 경우 왼쪽의 트리 뷰에서 새 데이터베이스를 선택 해야 합니다.

![WebMatrix 리본의 ' 새 테이블 ' 단추](displaying-data/_static/image5.png)

위쪽의 텍스트 상자 (워터 마크에 "테이블 이름 입력"이 표시 됨)에 "영화"를 입력 합니다.

![WebMatrix 데이터베이스 디자이너에 테이블 이름 입력](displaying-data/_static/image6.png)

테이블 이름 아래의 창은 개별 열을 정의 하는 위치입니다. 이 자습서의 *영화* 테이블에는 *ID*, *제목*, *장르*및 *연도*열만 생성 합니다.

**이름** 상자에 "ID"를 입력 합니다. 여기에 값을 입력 하면 새 열에 대 한 모든 컨트롤이 활성화 됩니다.

**데이터 형식** 목록으로 탭 하 고 **int**를 선택 합니다. 이 값은 ID 열에 정수 (숫자) 데이터를 포함 하도록 지정 합니다.

> [!NOTE]
> 여기서는 더 이상 호출 하지 않지만 표준 Windows 키보드 제스처를 사용 하 여이 표를 탐색할 수 있습니다. 예를 들어 필드 사이에서 tab 키를 누르면 목록에서 항목을 선택 하기 위해 입력을 시작할 수 있습니다.

**기본값** 상자를 지나서 탭 합니다. 즉, 비워 둡니다. **기본 키** 로 Tab 키를 누르고 선택 합니다. 이 옵션은 *ID* 열에 개별 행을 식별 하는 데이터가 포함 되도록 데이터베이스에 지시 합니다. 즉, 각 행에는 해당 행을 찾는 데 사용할 수 있는 ID 열에 고유한 값이 있습니다.

Id 옵션 **을** 선택 합니다. 이 옵션은 데이터베이스에 각 새 행에 대 한 다음 일련 번호를 자동으로 생성 하도록 지시 합니다. Is **Identity** 옵션은 **기본 키** 옵션을 선택한 경우에만 사용할 수 있습니다.

다음 표 형태 창의 행을 클릭 하거나 Tab 키를 두 번 눌러 현재 행을 마칩니다. 두 제스처는 현재 행을 저장 하 고 다음 항목을 시작 합니다. 이제 **기본값** 열에 **Null**이 표시 됩니다. 기본값은 기본값입니다 .이 경우에는에 대 한 값입니다.

새 **ID** 열을 정의 하는 작업을 마치면 디자이너는 다음 그림과 같이 표시 됩니다.

![WebMatrix 데이터베이스 디자이너에서 영화 테이블의 ID 열을 정의한 후](displaying-data/_static/image7.png)

다음 열을 만들려면 **이름** 열에서 상자를 클릭 합니다. 열에 대해 "Title"을 입력 한 다음 **데이터 형식** 값으로 **nvarchar** 를 선택 합니다. **Nvarchar** 의 "var" 부분은이 열에 대 한 데이터가 레코드에 대 한 레코드에 따라 달라질 수 있는 문자열이 되도록 데이터베이스에 지시 합니다. "N" 접두사는 "국가"를 나타내며이는 필드에서 문자 데이터를 입력할 수 있습니다. 즉, 필드에 유니코드 데이터가 포함 됩니다.

**Nvarchar**를 선택 하면 필드의 최대 길이를 입력할 수 있는 다른 상자가 표시 됩니다. 50을 입력 합니다 .이 자습서에서 작업할 영화 제목은 50 자 보다 깁니다.

**기본값** 을 건너뛰고 **null 허용** 옵션을 선택 취소 합니다. 데이터베이스가 제목이 없는 데이터베이스에 입력 되는 것을 허용 하지 않습니다.

작업을 완료 하 고 다음 행으로 이동 하면 디자이너는 다음 그림과 같습니다.

![WebMatrix 데이터베이스 디자이너에서 영화 테이블의 제목 열을 정의한 후](displaying-data/_static/image8.png)

이러한 단계를 반복 하 여 길이를 제외 하 고 "장르" 라는 열을 만들어 30으로 설정 합니다.

"Year" 라는 다른 열을 만듭니다. 데이터 형식에 대해 **nchar** ( **nvarchar**아님)를 선택 하 고 길이를 4로 설정 합니다. 연도에는 "1995" 또는 "2010"과 같은 4 자리 숫자를 사용 하므로 가변 크기 열이 필요 하지 않습니다.

완성 된 디자인은 다음과 같습니다.

![모든 필드가 영화 테이블에 대해 정의 된 후 WebMatrix 데이터베이스 디자이너](displaying-data/_static/image9.png)

Ctrl + S를 누르거나 빠른 실행 도구 모음에서 **저장** 단추를 클릭 합니다. 탭을 닫아 데이터베이스 디자이너를 닫습니다.

## <a name="adding-some-example-data"></a>몇 가지 예제 데이터 추가

이 자습서 시리즈의 뒷부분에서는 폼에 새 동영상을 입력할 수 있는 페이지를 만듭니다. 그러나 지금은 페이지에 표시할 수 있는 몇 가지 예제 데이터를 추가할 수 있습니다.

WebMatrix의 **데이터베이스** 작업 영역에서 이전에 만든 *.sdf* 파일을 보여 주는 트리가 있습니다. 새 *.sdf* 파일의 노드를 연 다음 **테이블** 노드를 엽니다.

![트리를 사용 하 여 영화를 연 WebMatrix 데이터베이스 작업 영역 테이블](displaying-data/_static/image10.png)

**동영상** 노드를 마우스 오른쪽 단추로 클릭 한 다음 **데이터**를 선택 합니다. WebMatrix는 *동영상* 테이블에 대 한 데이터를 입력할 수 있는 그리드를 엽니다.

![WebMatrix의 데이터베이스 항목 그리드 (비어 있음)](displaying-data/_static/image11.png)

**Title** 열을 클릭 하 고 "When Harry Sally"을 입력 합니다. Tab 키를 사용 하 여 **장르** 열로 이동 하 고 "Romantic 코미디"를 입력 합니다. **Year** 열로 이동 하 고 "1989"을 입력 합니다.

![한 레코드를 포함 하는 WebMatrix의 데이터베이스 입력 표](displaying-data/_static/image12.png)

Enter 키를 누르고, WebMatrix는 새 영화를 저장 합니다. **ID** 열이 채워져 있는지 확인 합니다.

![하나의 레코드와 자동 생성 된 ID가 있는 WebMatrix의 데이터베이스 입력 그리드](displaying-data/_static/image13.png)

다른 영화를 입력 합니다 (예: "바람", "드라마", "1939"). ID 열이 다시 채워집니다.

![두 개의 레코드와 자동 생성 Id가 있는 WebMatrix의 데이터베이스 입력 표](displaying-data/_static/image14.png)

세 번째 영화를 입력 합니다 (예: "Ghostbusters", "코미디"). 실험으로 **Year** 열을 비워 두고 enter 키를 누릅니다. **Null 허용** 옵션을 선택 취소 했기 때문에 데이터베이스에 오류가 표시 됩니다.

![필요한 열 값을 비워 두면 ' 잘못 된 데이터 ' 오류가 표시 됨](displaying-data/_static/image15.png)

**확인** 을 클릭 하 여 뒤로 돌아가서 항목 ("Ghostbusters"의 연도는 1984)을 수정 하 고 enter 키를 누릅니다.

8 가지가 될 때까지 여러 영화를 입력 합니다. (8을 입력 하면 나중에 페이징 작업을 더 쉽게 수행할 수 있습니다. 그러나 너무 많은 경우 지금은 몇 가지만 입력 하세요. 실제 데이터는 중요 하지 않습니다.

![두 레코드를 포함 하는 WebMatrix의 데이터베이스 입력 표](displaying-data/_static/image16.png)

모든 영화를 오류 없이 입력 한 경우 ID 값은 순차적입니다. 불완전 한 동영상 레코드를 저장 하려고 하면 ID 번호가 순차적이 지 않을 수 있습니다. 그렇다면 이것이 괜찮습니다. 이 숫자에는 내재 된 의미가 없으며, 중요 한 것은 *동영상* 테이블에서 고유 하다는 점입니다.

데이터베이스 디자이너를 포함 하는 탭을 닫습니다.

이제이 데이터를 웹 페이지에 표시 하도록 전환할 수 있습니다.

## <a name="displaying-data-in-a-page-by-using-the-webgrid-helper"></a>WebGrid 도우미를 사용 하 여 페이지에 데이터 표시

페이지에 데이터를 표시 하려면 `WebGrid` 도우미를 사용 합니다. 이 도우미는 표 또는 테이블 (행 및 열)에 표시를 생성 합니다. 여기에서 볼 수 있듯이 서식 지정 및 기타 기능을 사용 하 여 그리드를 구체화할 수 있습니다.

표를 실행 하려면 몇 줄의 코드를 작성 해야 합니다. 이러한 몇 가지 줄은이 자습서에서 수행 하는 거의 모든 데이터 액세스에 대 한 일종의 패턴으로 사용 됩니다.

> [!NOTE]
> 실제로는 페이지에 데이터를 표시 하는 다양 한 옵션이 있습니다. `WebGrid` 도우미는 하나 뿐입니다. 이 자습서에서는 데이터를 표시 하는 가장 쉬운 방법 이며 매우 유연 하기 때문에이 자습서를 선택 했습니다. 다음 자습서 집합에서 페이지의 데이터로 작업 하는 "수동" 방법을 사용 하 여 데이터를 표시 하는 방법에 대 한 직접 제어를 제공 하는 방법을 확인할 수 있습니다.

WebMatrix의 왼쪽 창에서 **파일** 작업 영역을 클릭 합니다.

만든 새 데이터베이스는 *App\_Data* 폴더에 있습니다. 폴더가 아직 없는 경우 WebMatrix는 새 데이터베이스에 대해 해당 폴더를 만들었습니다. 이전에 도우미를 설치 했을 때 폴더가 존재 했을 수 있습니다.

트리 뷰에서 웹 사이트의 루트를 선택 합니다. 웹 사이트의 루트를 선택 해야 합니다. 그렇지 않으면 새 파일이 App\_Data 폴더에 추가 될 수 있습니다.

리본에서 **새로 만들기**를 클릭 합니다. **파일 형식 선택** 상자에서 **CSHTML**를 선택 합니다.

**이름** 상자에서 새 페이지의 이름을 "동영상이. cshtml"로 합니다.

![' 동영상 ' 페이지를 표시 하는 ' 파일 형식 선택 ' 대화 상자](displaying-data/_static/image17.png)

**확인** 단추를 클릭합니다. WebMatrix는 일부 기본 요소가 포함 된 새 파일을 엽니다. 먼저 데이터베이스에서 데이터를 가져오는 코드를 작성 합니다. 그런 다음 페이지에 태그를 추가 하 여 실제로 데이터를 표시 합니다.

### <a name="writing-the-data-query-code"></a>데이터 쿼리 코드 작성

페이지 맨 위에서 `@{`와 `}` 문자 사이에 다음 코드를 입력 합니다. (여는 중괄호와 닫는 중괄호 사이에이 코드를 입력 해야 합니다.)

[!code-csharp[Main](displaying-data/samples/sample1.cs)]

첫 번째 줄은 앞에서 만든 데이터베이스를 엽니다 .이 데이터베이스는 데이터베이스를 사용 하 여 작업을 수행 하기 전에 항상 첫 번째 단계입니다. 열려는 데이터베이스의 `Database.Open` 메서드 이름을 알려 줍니다. 이름에는 *.sdf* 를 포함 하지 않습니다. `Open` 메서드는 *.sdf* 파일 (즉, *webpagesmovies .sdf*)을 찾고 있으며 *.sdf* 파일이 *App\_Data* 폴더에 있는지를 전제로 합니다. (이전에는 *앱\_데이터* 폴더를 예약 했습니다 .이 시나리오는 ASP.NET에서 해당 이름에 대 한 가정을 수행 하는 위치 중 하나입니다.)

데이터베이스를 열 때이에 대 한 참조는 `db`라는 변수에 저장 됩니다. (임의의 이름을 지정할 수 있음) `db` 변수는 데이터베이스와의 상호 작용을 종료 하는 방법입니다.

두 번째 줄은 실제로 `Query` 메서드를 사용 하 여 데이터베이스 데이터를 인출 합니다. 이 코드가 작동 하는 방식 확인: `db` 변수는 열린 데이터베이스에 대 한 참조를 포함 하 고 `db` 변수 (`db.Query`)를 사용 하 여 `Query` 메서드를 호출 합니다.

쿼리 자체는 SQL `Select` 문입니다. SQL에 대 한 약간의 배경 정보는 뒷부분에서 설명 합니다. 문에서 `Movies`는 쿼리할 테이블을 식별 합니다. `*` 문자는 쿼리에서 테이블의 모든 열을 반환 하도록 지정 합니다. 열을 쉼표로 구분 하 여 나열할 수도 있습니다.

쿼리의 결과 (있는 경우)가 반환 되 고 `selectedData` 변수에서 사용할 수 있게 됩니다. 마찬가지로 변수 이름을 지정할 수 있습니다.

마지막으로 세 번째 줄은 `WebGrid` 도우미의 인스턴스를 사용 하도록 ASP.NET에 지시 합니다. `new` 키워드를사용 하 여 도우미 개체를 만들고 `selectedData` 변수를 통해 쿼리 결과를 전달 합니다. 새 `WebGrid` 개체는 데이터베이스 쿼리의 결과와 함께 `grid` 변수에 제공 됩니다. 실제로 페이지에 데이터를 표시 하는 데 시간이 조금 더 필요 합니다.

이 단계에서 데이터베이스가 열리고, 원하는 데이터를 얻 었으 며, 해당 데이터를 사용 하 여 `WebGrid` 도우미를 준비 했습니다. 다음으로 페이지에서 태그를 만듭니다.

> [!TIP] 
> 
> **SQL(구조적 쿼리 언어)**
> 
> SQL은 데이터베이스에서 데이터를 관리 하기 위한 대부분의 관계형 데이터베이스에 사용 되는 언어입니다. 여기에는 데이터를 검색 하 고 업데이트 하는 데 사용할 수 있는 명령이 포함 되어 있으며,이를 통해 데이터베이스 테이블에서 데이터를 만들고 수정 하 고 관리할 수 있습니다. SQL은 프로그래밍 언어 (예 C#:)와 다릅니다. SQL을 사용 하면 데이터베이스에서 원하는 작업을 수행 하 고, 데이터를 가져오거나 작업을 수행 하는 방법을 파악 하는 데이터베이스 작업입니다. 다음은 몇 가지 SQL 명령의 예입니다.
> 
> `Select * From Movies`
> 
> `SELECT ID, Name, Price FROM Product WHERE Price > 10.00 ORDER BY Name`
> 
> 첫 번째 `Select` 문은 *동영상* 테이블에서 `*`로 지정 된 모든 열을 가져옵니다.
> 
> 두 번째 `Select` 문은 *Product* 테이블에서 price 열 값이 10 보다 많은 레코드에서 ID, 이름 및 가격 열을 인출 합니다. 이 명령은 Name 열의 값을 기준으로 결과를 알파벳 순서로 반환 합니다. 가격 기준과 일치 하는 레코드가 없는 경우이 명령은 빈 집합을 반환 합니다.
> 
> `INSERT INTO Product (Name, Description, Price) VALUES ('Croissant', 'A flaky delight', 1.99)`
> 
> 이 명령은 *Product* 테이블에 새 레코드를 삽입 하 고 Name 열을 "크 라 상"로 설정 하 고 Description 열을 "a 부실 즐거움"로 설정 하 고 price를 1.99으로 설정 합니다.
> 
> 숫자가 아닌 값을 지정 하는 경우이 값은에서 C#와 같이 작은따옴표로 묶여 있습니다. 이러한 따옴표는 텍스트 또는 날짜 값에는 사용할 수 있지만 숫자 앞뒤에는 사용할 수 없습니다.
> 
> `DELETE FROM Product WHERE ExpirationDate < '01/01/2008'`
> 
> 이 명령은 만료 날짜 열이 2008 년 1 월 1 일 이전인 *Product* 테이블의 레코드를 삭제 합니다. 이 명령은 *Product* 테이블에 이러한 열이 있다고 가정 합니다. 날짜는 여기에 MM/DD/YYYY 형식으로 입력 되지만 로캘에 사용 되는 형식으로 입력 해야 합니다.
> 
> `Insert` 및 `Delete` 명령은 결과 집합을 반환 하지 않습니다. 대신 명령의 영향을 받은 레코드 수를 나타내는 숫자를 반환 합니다.
> 
> 이러한 작업 중 일부 (예: 레코드 삽입 및 삭제)의 경우 작업을 요청 하는 프로세스에 데이터베이스에 대 한 적절 한 권한이 있어야 합니다. 따라서 프로덕션 데이터베이스의 경우 데이터베이스에 연결할 때 사용자 이름과 암호를 제공 해야 하는 경우가 많습니다.
> 
> 수십 개의 SQL 명령이 있지만 모두 여기에 표시 되는 명령과 같은 패턴을 따릅니다. SQL 명령을 사용 하 여 데이터베이스 테이블을 만들고, 테이블의 레코드 수를 계산 하 고, 가격을 계산 하 고, 더 많은 작업을 수행할 수 있습니다.

### <a name="adding-markup-to-display-the-data"></a>데이터를 표시 하는 태그 추가

`<head>` 요소 내에서 `<title>` 요소의 내용을 "영화"로 설정 합니다.

[!code-html[Main](displaying-data/samples/sample2.html?highlight=3)]

페이지의 `<body>` 요소 내에 다음을 추가 합니다.

[!code-html[Main](displaying-data/samples/sample3.html)]

이것으로 끝입니다. `grid` 변수는 이전 코드에서 `WebGrid` 개체를 만들 때 만든 값입니다.

WebMatrix 트리 보기에서 페이지를 마우스 오른쪽 단추로 클릭 하 고 **브라우저에서 시작**을 선택 합니다. 이 페이지와 유사한 내용이 표시 됩니다.

![동영상 테이블의 기본 WebGrid 도우미 출력](displaying-data/_static/image18.png)

열 머리글 링크를 클릭 하 여 해당 열을 기준으로 정렬 합니다. 제목을 클릭 하 여 정렬할 수 있는 기능은 **Webgrid** 도우미에 기본 제공 되는 기능입니다.

`GetHtml` 메서드는 이름에서 알 때 데이터를 표시 하는 태그를 생성 합니다. 기본적으로 `GetHtml` 메서드는 HTML `<table>` 요소를 생성 합니다. (원하는 경우 브라우저에서 페이지의 원본을 확인 하 여 렌더링을 확인할 수 있습니다.)

## <a name="modifying-the-look-of-the-grid"></a>그리드 모양 수정

방금 했던 것 처럼 `WebGrid` 도우미를 사용 하는 것은 쉽지만 결과 표시는 일반입니다. `WebGrid` 도우미에는 데이터가 표시 되는 방식을 제어할 수 있는 모든 종류의 옵션이 있습니다. 이 자습서에서 살펴볼 수 있는 방법은 너무 많습니다 .이 섹션에서는 이러한 옵션 중 일부에 대해 설명 합니다. 몇 가지 추가 옵션은이 시리즈의 이후 자습서에서 설명 합니다.

### <a name="specifying-individual-columns-to-display"></a>표시할 개별 열 지정

시작 하려면 특정 열만 표시 하도록 지정할 수 있습니다. 기본적으로 표 형태 창에는 *동영상* 테이블의 네 열이 모두 표시 됩니다.

*동영상과* 파일에서 방금 추가한 `@grid.GetHtml()` 태그를 다음과 같이 바꿉니다.

[!code-css[Main](displaying-data/samples/sample4.css)]

도우미에 표시할 열을 알리기 위해 `GetHtml` 메서드에 `columns` 매개 변수를 포함 하 고 열 컬렉션을 전달 합니다. 컬렉션에서 포함할 각 열을 지정 합니다. `grid.Column` 개체를 포함 하 여 표시할 개별 열을 지정 하 고 원하는 데이터 열의 이름을 전달 합니다. 이러한 열은 SQL 쿼리 결과에 포함 되어야 합니다. `WebGrid` 도우미가 쿼리에서 반환 되지 않은 열을 표시할 수 없습니다.

브라우저에서 *동영상과* 페이지를 다시 시작 합니다. 이번에는 다음과 같은 표시를 얻을 수 있습니다 (ID 열이 표시 되지 않음).

![선택한 열만 표시 하는 WebGrid 표시](displaying-data/_static/image19.png)

### <a name="changing-the-look-of-the-grid"></a>표 모양 변경

열을 표시 하기 위한 몇 가지 옵션이 몇 가지 있습니다 .이 중 일부는이 집합의 이후 자습서에서 살펴봅니다. 현재이 섹션에서는 표를 전체적으로 스타일을 지정할 수 있는 방법을 소개 합니다.

페이지의 `<head>` 섹션 내에서 닫는 `</head>` 태그 바로 앞에 다음 `<style>` 요소를 추가 합니다.

[!code-css[Main](displaying-data/samples/sample5.css)]

이 CSS 태그는 `grid`, `head`등의 클래스를 정의 합니다. 이러한 스타일 정의를 별도의 *.css* 파일에 넣고 페이지에 연결할 수도 있습니다. 실제로는이 자습서의 뒷부분에서이 작업을 수행 합니다. 그러나이 자습서의 작업을 쉽게 수행 하기 위해 데이터를 표시 하는 동일한 페이지 내에 있습니다.

이제 `WebGrid` 도우미를 가져와 이러한 스타일 클래스를 사용할 수 있습니다. 도우미에는이 목적에 대 한 다양 한 속성 (예: `tableStyle`)이 있습니다. 여기에는 CSS 스타일 클래스 이름을 할당 하 고 해당 클래스 이름은 도우미가 렌더링 하는 태그의 일부로 렌더링 됩니다.

이제이 코드 처럼 보이도록 `grid.GetHtml` 태그를 변경 합니다.

[!code-css[Main](displaying-data/samples/sample6.css)]

`tableStyle`, `headerStyle`및 `alternatingRowStyle` 매개 변수를 `GetHtml` 메서드에 추가 했으므로 여기에서 새로운 기능을 사용할 수 있습니다. 이러한 매개 변수는 잠시 전에 추가한 CSS 스타일의 이름으로 설정 되었습니다.

페이지를 실행 합니다. 이번에는 이전 보다 훨씬 단순한 표 모양의 표가 표시 됩니다.

![CSS 클래스 이름으로 설정 된 매개 변수를 포함 하는 WebGrid 표시](displaying-data/_static/image20.png)

`GetHtml` 메서드가 생성 된 것을 확인 하기 위해 브라우저에서 페이지의 소스를 살펴볼 수 있습니다. 여기서는 자세히 다루지 않지만 `tableStyle`와 같은 매개 변수를 지정 하 여 표를 통해 다음과 같은 HTML 태그를 생성 하는 것이 중요 합니다.

`<table class="grid">`

`<table>` 태그에는 이전에 추가한 CSS 규칙 중 하나를 참조 하는 `class` 특성이 추가 되었습니다. 이 코드는 `GetHtml` 메서드에 서로 다른 매개 변수를 &mdash; 하 여 메서드가 태그와 함께 생성 하는 CSS 클래스를 참조할 수 있도록 하는 기본 패턴을 보여 줍니다. CSS 클래스를 사용 하 여 수행 하는 작업은 사용자에 게 있습니다.

## <a name="adding-paging"></a>페이징 추가

이 자습서의 마지막 작업으로, 표에 페이징을 추가 합니다. 지금은 모든 영화를 한 번에 표시 하는 것은 문제가 되지 않습니다. 그러나 수백 개의 영화를 추가한 경우 페이지 표시 시간이 오래 걸릴 수 있습니다.

페이지 코드에서 `WebGrid` 개체를 만드는 줄을 다음 코드로 변경 합니다.

[!code-csharp[Main](displaying-data/samples/sample7.cs)]

이전과의 유일한 차이점은 3으로 설정 된 `rowsPerPage` 매개 변수를 추가 했다는 것입니다.

페이지를 실행 합니다. 표는 한 번에 3 개의 행을 표시 하 고 데이터베이스의 영화를 통해 페이지를 이동할 수 있는 탐색 링크를 표시 합니다.

![페이징을 사용 하 여 WebGrid 표시](displaying-data/_static/image21.png)

## <a name="coming-up-next"></a>다음에서 시작

다음 자습서에서는 Razor 및 C# 코드를 사용 하 여 양식에서 사용자 입력을 가져오는 방법을 배웁니다. 제목 또는 장르로 영화를 찾을 수 있도록 동영상 페이지에 검색 상자를 추가 합니다.

## <a name="complete-listing-for-movies-page"></a>동영상에 대 한 전체 목록 페이지

[!code-cshtml[Main](displaying-data/samples/sample8.cshtml)]

## <a name="additional-resources"></a>추가 리소스

- [Razor 구문을 사용한 ASP.NET 웹 프로그래밍 소개](https://go.microsoft.com/fwlink/?LinkID=202890)

> [!div class="step-by-step"]
> [이전](intro-to-web-pages-programming.md)
> [다음](form-basics.md)
